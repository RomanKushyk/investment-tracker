# design/extensions/ — post-v1 design references

Extension references per decision D14 and pipeline G7 (`docs/DECISIONS.md`, Design
pipeline): a **separate Claude design session** turns each phase's brief into
one or more `<surface>.dc.html` files here. These are the ONLY files ever added under
`design/`; the three original handoff files stay immutable.

## Files

| File | Phase | Purpose |
|------|-------|---------|
| `settings.dc.html` | 2 | `/settings` screen: sidebar Settings group + pill (S1), the 4 section cards (S2), targets editor + Σ pill (S4), dataset switch + DEMO badge (S5), destructive typed-name dialogs (S6), relocated Backup (S7), editable ₴/$ rate (S8). **Header comment mints the `--color-warn/-tint/-tint-text` token values** — the single source for the phase's new token family. |
| `asset-form.dc.html` | 2 | AssetForm (S3): create + edit dialogs, fixed-coupon group, Inzhur link group (toggle reveal, Fund/Bond kind, slug/ISIN ref, units-first framing), validation states + full message vocabulary, TransactionPanel quick-create context. |
| `metrics-exposure.dc.html` | 2 | Audited-metrics exposure fragments: Overview KPI relabel + "Total return (net)" card + income net-of-tax line (S9a), Yield Total return/XIRR columns (S9b), Portfolio "Capital gain" relabel (S9c), ledger-drift warn chip (S9d), TransactionPanel Withdrawal/Redemption types (S10). |
| `daily-quotes-live.dc.html` | 3 | Living `/` screen: Fetch-quotes button 5-state machine + demo tag (S1), per-row `auto`/`manual`/`stale` provenance chips (S2), dirty-field "Use fetched?" offer (S3), ghost "suggested" accrual input vs the other three input states (S4), coupon-due suggestion card above the Transaction panel (S5). **Header comment pins the SUGGESTION VISUAL LANGUAGE** (dashed = proposed · solid+ink = the user's · pos-border = valid/saved) that S3/S4/S5 share. |
| `reminders.dc.html` | 3 | ReminderStrip on `/` and `/overview` (S6): banner anatomy, the 3 severities, stacking + the 3-banner cap and "+N more", dismiss/empty/restore paths, app-open toast. **Header comment mints the `--color-neg-tint` / `--color-neg-tint-text` values** — the single source for the phase's new token family (overdue severity only). |
| `automation.dc.html` | 3 | AssetForm Inzhur ref live picker — loading / loaded / error→manual / empty / stale / demo / selected (S7); Settings → Automation card filled in inside the settings card stack — `autoQuoteSuggest`, `couponSuggest`, reminders switch + lead-days sub-row + restore-dismissed sub-row, with their off/error/disabled states (S8). |
| `data-portability.dc.html` | 4 | The FINISHED Settings → Data card (S1) drawn complete in BOTH datasets — final row order Dataset → Backup → Import → Spreadsheet export (CSV) → Keep a file in sync → Danger zone; the import row with its **solid** drop target and its file-level rejections (S2); the CSV export button group + format/column notes (S5); the Data-card side of the CSV-import deltas incl. the empty-cell-≠-0 illustration (S6); all seven file-mirror states, the absent row on non-Chromium and the demo-disabled tag (S7); the danger-zone sub-panel (S8). **Header comment restates the widened `neg-tint`/`neg-tint-text` rule verbatim** and names its exactly-two sites — no token is minted in Phase 4. |
| `import-dialog.dc.html` | 4 | The import preview & diff dialog (S3) in its meaningful states — preview with a diff, preview with warnings + the settings opt-in, pending `Replacing…` with the write-lock wait, plus error/success annotations — the CSV variant with scope strip, detection line, snapshot-scoped banner and `Replace snapshots` (S6 dialog side), and the rejected-file report with single-reason forms and the row-error list (S4). **Pins the phase's ONE dashed element:** the diff panel (proposed data, crossed only by the confirm press). Same widened-token header as its sibling. |
| `appearance-language.dc.html` | 5 | Theme control Light / Dark / System (S1), language control Українська / English (S2), the complete dark palette sheet — all 57 tokens with **measured** WCAG ratios (S3), charts in dark (S4), Ukrainian copy in situ (S5), plus the finished Appearance card drawn in every theme. **Header comment carries FINDING 1:** neither brand face contains a single Cyrillic glyph, which is what forced the D54/D55 font change. **Amended 2026-08-12 (D56):** all 231 capsules rewritten to the radius rule and its 23 segmented tracks made concentric with their segments, measured off the file's own rendered boxes — colours, spacing, copy and states untouched. Read shape from `README.md` §4, never from this file's original capsules. |
| `scroll-surface.dc.html` | 6 | **Supersedes the scrollbar half of `mobile.dc.html` S5** (brief § S7, decision D65): the 12 px rail at 6× and at size (T1) with the two withdrawn insets kept and the reason they died; the gutter and why it is the PARENT's padding, drawn as the failure it prevents, plus the three debts a scrollport creates — percentage heights, focus rings, keyboard scrolling (T2); the dialog as three bands, at rest and scrolled, and why a grid rather than a flex column (T3). Mints no token. |
| `mobile.dc.html` | 6 | The mobile shell: the collapsible sidebar drawn open and closed (S1), the header bar that stands in for it (S2), the record card that answers all four tables (S3), daily quotes with the keyboard up (S4), the four overlays at 360 including the ScrollArea thumb that replaces the platform scrollbar (S5), and tap-to-pin on the charts (S6). **S5's scrollbar half is SUPERSEDED by `scroll-surface.dc.html`** (D65) — its other three overlays and every other surface here still stand. **Header comment mints `--color-scrim`** with both theme values — and records that its dark value cannot carry the drawer's boundary (1.02:1), so the drawer takes a `sidebar-muted` edge at 5.51:1. |
| `where-things-live.dc.html` | 7 | The Phase 7 information architecture: the rule that a page is editable where it shows STORED data (T0); the edit affordance in its two G-2 variants — batch `Cancel + Save` on `/allocation`, per-entity `Done` on `/portfolio` — drawn in both themes (S1); targets rehoused into the current-vs-target card (S2); asset actions on a `RecordCard` footer band (S3); the `Entry` group and `/transactions`, including what `/` becomes on a day with no coupon cards (S4); and the two collapse controls drawn on ONE panel so the difference is visible rather than argued (S5). **Mints no token.** Header carries the seven places the brief could not be drawn as written (F1–F7) and the one deliberate D56 exception, argued. |
| `period-and-analytics.dc.html` | 8 | The period, and the three screens under it. **S1 — the period control lives in `ScreenHeader`'s ACTION SLOT, not the sidebar**: below `md` the sidebar is a Radix `Dialog` behind a scrim, so a control that reframes what you are looking at sits on top of it and gives no feedback at the press; and it acts on 3 of 11 routes, which are the 1st, 4th and 6th of an eight-item group. A `Select`, not a segmented track — six Ukrainian options measure a 622 px track against 336 at 360. **S5 — the portfolio XIRR (A25, computed since 2026-08-18 and shown nowhere) goes on `/overview`'s `Загальна дохідність (чиста)` KPI as a second sub-line**, argued from BOUNDARIES: every `/yield` column is measured at the asset boundary and `portfolioXirr` entirely at the external-capital one, so it has no honest cell in that table in any row. Also S2 `/yield` under a window, S3 `/overview`'s time dimension, S4 `/seasonality`'s month axis, S6 readability. **Its findings are worth more than its drawings** — F-1: four of six period options resolve to the SAME window on the seed (174 d, three of them `clamped`), so the control has six labels and three behaviours; F-2: `annualizedPct` is LINEAR, so a 30-day window multiplies by 12,17 and `Річна` triples across windows while Δ barely moves; F-14: `warn` #8f6b33 is 4,46 : 1 on `page` and fails 1.4.3 for small text, which `ParseSkips.tsx:61` already violates; F-15: a `Select`'s popover is locked to its trigger width, so a hint widens the trigger. **THREE review rounds (15 + 15 + 12) ran on it, and the third's lesson is written into the sheet**: a design extension owns geometry, colour, motion, state and copy — NOT `core/` semantics. Every derivation rule it once stated as canonical is demoted to a requirement naming the shipped function the implementer must derive and test against. |
| `screen-density.dc.html` | — | **The `/` screen's density, from the owner's report that it looks empty (A34).** **D-1 — the composition is capped at 944 and CENTRED**, because 944 = 560 + 24 + 360 is the one width where both column caps land exactly and the row keeps zero slack; between 944 and 884 the rail alone gives (360 → 300) and the quote rows never move. The two rejected spendings are drawn, not argued: a 540 rail measures **417,5 px** between a label and its figure — the 440 px void rebuilt one column to the right — and a left-aligned composition leaves the header overhanging its own content by 180. **D-2 — the title block leaves the ritual column**: it needs 871,27 px on one line, a 560 column is 311,27 short (87 px, two lines, the fetch button beside the date), and at today's 884 it holds by **12,73 px**. **D-4 — the pending-change block is taken**, naming the CHANGE and never the total (G-8). **D-5 amends the brief's acceptance**: the yield card is repaired at 360 too, where it ships as **246,5 px, eleven lines, `Дохідніс/ть` split mid-word**. **Two findings contradict the brief**: F-4, its `< 60 px` target is unreachable at its own 560 (`void = card − 444,2`; measured 115,8, and < 60 needs a card under 504 — owner ruled 560 stands and the target is restated), and F-6, its unconditional caps leave **214 / 414 px** of hole at a 774 container, the failure `DailyQuotes.tsx` already documents for the aside. Mints no token, adds no radius. |
| `muted-legibility.dc.html` | — | **The `muted` step, re-derived (D68).** The defect drawn at the sizes it actually occurs — `muted` #8b8a86 measured on the rendered app at **2.88 on `panel`, 3.17 on `page`, 3.46 on `card`** across 258 elements and ten routes, none of which reaches WCAG's large-text exemption (T1); the arithmetic that picks `#696865` over the two lighter candidates, including the one that lands exactly ON the 4.5 bar (T2); `label` retired into `muted`, because two tokens that must hold the same luminance to be legible are one step drawn twice (T3); and what does NOT move — the whole dark sheet, `faint`, the asset hues (T4). **Mints no hue** — it moves one token down its own warm axis and deletes another. Light theme only: dark measures 5.56–6.53 and is untouched. |
| `field-border.dc.html` | — | **The field edge, re-derived (issue #80, implemented by #3).** The owner reported invisible input borders in dark; measured on the rendered app the defect is in BOTH themes, and the lowest reading in the survey is a light one — `hairline` on `panel` at **1.03 : 1**, from the six `/transactions` fields that sit on `card` inside a `panel`, which is why `panel` is the surface every candidate is scored on. **Mints `--color-field-border`** (`#84827d` light · `#747169` dark, 3.20 / 3.21 on panel) **and re-values `--color-pos-border`** (`#6f8567` · `#5c7f53`) — the saved edge has to move or repairing the idle one inverts the pair, so a row would appear to lose its boundary the moment its value is accepted. **The file's one real decision is why a new token, when its dark value lands 1.06 : 1 from `faint`.** Both alternatives were carried to the end. Minting by threshold search and not noticing the collision was the first attempt, correctly rejected. Moving `faint` was the second, and it costs three things that are measured and unrecoverable: it silently reverses `Scroller.tsx:47-51`, which puts the resting thumb below 3 : 1 **deliberately** and argues it; it breaks the drawer's dragged thumb to **1.00 : 1**, because everything resting on `faint` would then need an `ink` hover and `[data-dark-surface]` overrides `panel-border`, `faint` and `muted` but **not `ink`**; and it compresses the light `faint`↔`muted` text ladder 2.63 → 1.45. All three come from one place — `faint` carries a TEXT rank and an INDICATOR rank, and a field edge is neither — so the ruling is stated as **two tokens that must be free to move independently are not redundant because they happen to share a luminance today**, the inverse of the case `muted-legibility.dc.html` settled. In light they are already 1.81 : 1 apart. **Hover unifies on `ink`** (3.93 / 4.06) because `muted` measures 1.45 from the new idle edge, smaller than the weakest hover shipping today (1.55); the step is *gentler* than the 12.19 the three `ink` sites already ship. **Dash narrows to controls**: the ghost input and the accept pill take the new token dashed, the three dashed containers keep `faint`. Because `faint` never moves, the thirteen text sites, both scrollbar thumbs, the chart stub and every `faint` figure in `index.css` and `Scroller.tsx` are untouched; three shipped comments do go stale (`AssetForm.tsx:67`, `Select.tsx:8` and `:9`) and are named in the acceptance. Checkboxes are ruled out on a measurement — no border-width utility and native `appearance: auto`. Popovers are #82; the `Switch` OFF track and the `Scroller` rail are #83. The fill-step disagreement between two merged sheets is recorded UNRESOLVED. Supersedes the field-edge treatment of ALL TEN merged drawings that draw a 36 px field with a `hairline` edge — the master reference, `daily-quotes-live`, `appearance-language`, `asset-form` (35 boxes), `where-things-live` (13), `settings` (8), `automation` (5), `screen-density` (5), `period-and-analytics` (5) and `metrics-exposure` (2) — because D14 gives each precedence over its own surfaces, and naming only three would have left `/assets`, `/settings` and `/allocation` with two live authorities. Edits none of the ten. |
| `switch-border.dc.html` | — | **The switch's boundary, re-derived (issue #83, implemented by #87).** #83 reported the `Switch` OFF *fill* at **1.03 : 1** on `panel`; measured, the *edge* is the defect and it fails in BOTH themes — `panel-border` at 1.14 / 1.37 light and 1.25 / 1.36 dark on panel / card, plus the knob's ring, which is that same token through `--shadow-thumb`, at 1.36 on the knob in dark. **Mints `--color-switch-border` as a NAME, not a value** (`var(--color-field-border)` in both blocks, no hex, palette 45 → 46): the edge goes to 3.20 / 3.84 light and 3.21 / 3.49 dark, and dark's `--shadow-thumb` re-points with it, 1.36 → 3.49. **The file's one real decision is why a NAME when the value is identical in both themes** — the mirror image of `field-border.dc.html`'s question, so "they must be free to diverge" is unavailable and the synonym is defended as one, against `index.css:177-183`'s "do not re-mint" (the distinguisher is *must*: `label`/`muted` were forced onto one luminance by one shared constraint; these two are held to the same *bar* by independent measurements and part when either fill moves). It also refuses the shortcut of pointing the markup at `field-border`, which no test would catch because `Switch.tsx` is not a `FIELD_FILE`. **What does not move:** `switch-track` keeps both values — a compliant *fill* is unaffordable, costing the OFF↔ON cue 12.19 → 4.20 in light and 7.28 → 4.06 in dark — so #83's 1.03 stands with that arithmetic as its reason; and **the `Scroller` rail is RE-AFFIRMED** at 1.37 / 1.36 / 1.20 / 1.53 as furniture that identifies nothing, with `faint`, `panel-border` and every literal in `scroll-surface.dc.html` untouched. What it does supersede there is **one clause of the argument** at `:126` (repeated at `mobile.dc.html:900`) — "the rail already marks where the control is" — because it makes the rail and the thumb vouch for each other; the exemption is left resting on *a scroll region is identified by its content* alone, and the file states outright that an affordance is not an identification requirement. **Supersession is narrow and that is the finding:** only `asset-form` `:137`/`:430` and `automation` `:365` draw the OFF edge at all — the other seven of the ten switches across those two sheets are ON and declare no border literal — and neither file is edited. `settings.dc.html` draws no switch despite `automation.dc.html:48-50` crediting it with the anatomy. The import dropzone, which #83 excluded and this sheet found orphaned, is now #88. |
| `parchment-5h.dc.html` | — | **The parchment palette and the 5h mark (brief #89, implemented by #91).** The design session's own output — its eyebrow reads *Turn 16* — re-wrapped in the house idiom: **fonts linked rather than embedded** (its own file was 746 KB, 683 KB of it embedded `data:font/woff2` faces, 2.7x the largest sheet here) and **every icon replaced with lucide's own path data** at lucide's own weight, from the set the app already ships and `Sidebar.tsx:1` already imports — so the drawn glyph and the imported component are one object rather than two that look alike. Nothing else is redrawn. **It re-values every existing colour in both themes** and mints **30 tokens** the app has no name for: accent x5 (with `selection` and `focus`), info x3, logo x3 plus a mono fallback, sb x16. **Its Mapping is the half the session does not have**: `src/index.css` declares 46 `--color-*` and 4 `--shadow-*` per theme and the session names about thirty, so `faint`, `ink-hover`, `switch-track`, `pos-border`, `pos-on-dark`, every `*-tint-text`, the four asset hues with their tints and the three light shadows are derived here — at a session — rather than picked inside whichever component needs them first. **The finding that shapes it: the session's own boundary greys cannot carry a component boundary on the session's own planes** — `border-strong` reads **1.44 : 1** on the light panel and **1.40 : 1** on the dark one against the 3 : 1 of WCAG 1.4.11, so the control-boundary rank takes the session's THIRD TEXT STEP instead (3.35 / 3.69 / 3.06 light, 4.20 / 3.94 / 3.65 dark) and the two greys below it stay decorative, which is what they already are. **`pos-border` is derived at the idle edge's luminance and not from `gain`**, because `field-border.test.ts` holds the saved edge within 1.10x of the idle one and the session's green is 1.7x away. **Light `panel` is the owner's call** (`bg-hover` #e7e0d5): the session draws three light layers going UP and none going down, and `panel` is a recess in light and a step up in dark. **Two things the new palette repairs by arriving, and neither is repaired completely**: three of the four asset hues clear 3 : 1 on EVERY plane in both themes, so the exemption at `index.css:413-422` — which cited the immutable master reference — is **narrowed** rather than retired; what is left is `reit` #b07a52 at **2.79 : 1 on the light panel**, recorded at the value because `ColorDot` and `ShareBar` paint the bare hue inside panels. And light `warn` goes **4.46 → 4.93** on `page`, the failure `period-and-analytics.dc.html` F-14 reported — like for like, and on the binding `panel` it clears 4.5 by **0.003**, which the mapping records as no margin at all. **Five pieces of furniture drawn here have never shipped** — the ⌘K field, the user footer, a count badge, the icon rail and a «Податки» route — kept rather than tidied away for the reason this file gives at length for `kubushka-mirror.json`, and each named in the header as NOT a requirement. **One of the seven usage rules has no consumer**: the monochrome-icon rule governs icons the nav does not have. **Four readings are the session's own values failing against the app's own text sizes, and the sheet does not resolve them**: `sb-label` at 3.09 : 1 under 9.5-10 px nav group headers, `sidebar-muted` at 4.33 under the version badge and every dark KPI label, light `accent` at 4.45 on the binding `panel` where usage rule 2 makes it every link, and light `warn` at 1.013 : 1 from light `accent` — a caution and the brand as one colour. Moving any of them moves the palette the design session shipped, so it went to the owner and **the ruling is to accept them**: the values stand and the figures are recorded at them, the way `reit`'s 2.79 and the switch's dark 2.05 are. The cost is stated rather than discovered — the sidebar's group headers and labels ship under 1.4.3, where the retired palette had them at 5.07 and 5.59, and the two alternatives (darken three steps; move the consumers onto `sb-item`) are costed in the sheet and declined. **Supersedes every colour in every merged drawing**, which is the whole point of it — the master reference draws light and `appearance-language.dc.html` dark, both re-valued — and supersedes **no** geometry, copy or state machine, so where it and an older sheet disagree about a size the older sheet still wins. `field-border`, `switch-border` and `muted-legibility` keep their rulings and lose only their numbers. Edits none of them. |
| `floating-edges.dc.html` | — | **The floating-surface edge, retired into the rank (brief #86, implemented by #98).** #82 repaired the dark boundary and left the light half in writing; #90 was adopted as #86's session but its Mapping only preserved the tokens' SHAPE — *"unchanged shape … so it follows"* — which is not an answer to whether 1.4.11 binds a light popover whose shadow does draw. **`--color-popover-edge`, `--color-toast-edge` and `--color-surface-edge` are RETIRED, not re-valued**, and their seven consumers read `field-border` — the rank `index.css` already calls the control-boundary rank — directly: the palette goes 74 → **71** names per block and no hex is minted. **The file's one real decision is whether the shadow can carry the boundary**, and it is arithmetic rather than taste: `--shadow-popover` bounds at a **1.24–1.25** step and `--shadow-dialog` at **1.39–1.41**, printed as UPPER bounds (full nominal alpha, the value *under* the occluded box; outside the edge the visible step is nearer 1.12 and 1.17), so the shadow describes what a sighted user perceives and is never a finding of conformance. **The ladder has a hole in it**: nothing sits between `faint` 1.75 and `field-border` 3.06 because the parchment session mints no fourth grey, so the choice is binary and no sixth candidate is offered — one would be a new grey and minting a grey is a palette decision this file has no standing to make. **Two surfaces foreclose "no change"**: a `Select` inside a `Dialog` is `card` on `card`, a **1.00** fill step behind a 1.34 stroke (3.94 in dark), and `CHART_TOOLTIP` carries **no shadow in either theme** at all four consumers, so its 1.44 is the whole boundary. **Why retire rather than re-value, which is this file's second draft and the first is deleted**: three names holding one value in BOTH themes are one step drawn three times, which `index.css`'s own retired `--color-label` comment rules on ("Two tokens that must hold the same value to be legible are one step drawn twice … Do not re-mint"). `--color-switch-border` is NOT retired with them although it is also `var(--color-field-border)` in both blocks: its name carries the dark OFF edge's 2.05 recorded shortfall, which has nowhere else to live, and the three retired here carry no record their rank does not already carry. **`--color-drawer-edge` is KEPT** — one consumer, the drawer over `--color-scrim` (NOT the `Dialog`'s `sidebar/40`), reading 5.50 / 5.10 / **4.77** by plane, identified without a stroke, and it genuinely differs per theme. **Readings are reported INWARD and OUTWARD**, because for a surface over the overlay they disagree: the `Dialog` panel and the mobile date sheet gain an identifiable 3.69 inward but their outward stroke is 1.20–1.39 on every plane, so they are **not fully repaired** and the table says so instead of stamping them — that remainder is **#99**, and #92 moves `sidebar`, which the overlay is 40 % of. **The cost is the desktop rail**: 3.35 outward on a plane already at 16.24, which `index.css` calls "decoration nothing asked for" and this file overrules, accepted because an exemption cannot be written — the `Dialog` panel and the date sheet are identical in fill, backdrop, fill step and both stroke readings while reading different tokens, and retiring the names removes the families that distinction turned on. **Supersedes 11 boxes across 5 sheets, enumerated, with a re-runnable survey signature whose blur floor is load-bearing** — sibling sheets' annotation cards carry `--shadow-card`'s 3px blur and without the floor the signature sweeps them in, which a first draft did: the old light `hairline` #e8e7e4 at `asset-form:309`, `automation:130 :144 :178 :204 :217`, `metrics-exposure:350`, `period-and-analytics:1661`, `mobile:961`; the old light `panel-border` #dedcd8 at `mobile:999` and `:1069`. `scroll-surface:171` (a diagram frame) and `appearance-language:1327` (dark) are excluded with reasons, **the master reference draws ZERO** and is untouched, and the file's own five boxes are computed rather than counted. Supersedes no geometry, no copy and no dark value. **Edits none of them.** |
| `dropzone-edge.dc.html` | — | **The import dropzone's rest edge, into the rank (brief #88, implemented by #88 — the owner ruled one branch and two commits).** The box was named four times and owned nowhere: `field-border.dc.html:38-39` and `:369` filed it into #83, #83's body put it out of scope, `switch-border.dc.html:40-42` found it orphaned and named it #88, and #98 excluded it by name — so this file exists to give it an owner. **Every figure the issue prints is pre-parchment** (1.14 / 1.37 / 1.25, on a border-and-fill pair the palette no longer contains); re-measured, the rest reads **1.44 in / 1.74 out light and 1.40 / 1.51 dark** — the box has exactly TWO adjacencies, its own `panel` fill and the `card` behind it, and `page` is not one of them. **The ruling: the rest joins the control-boundary rank** (3.06 / 3.69 light, 3.65 / 3.94 dark) **and the hover follows to `muted`** (5.28 / 6.36, 6.36 / 6.87). **Nothing is minted** — this edge holds no shortfall and no per-theme difference, so a name for it would be the rank's own value drawn twice, which is what #98 retired three names for; the palette stays at 71 per block and no hex moves. **The file's one real decision is not arithmetic**: whether *Design pipeline*'s open exemption — *"a region's decorative edge and furniture that identifies nothing"* — covers a box that the drawing and the code both call *"a container, not a pressable"* and that takes no focus, no `role` and no tab stop. Answered NO on two tests: furniture that identifies nothing has no states, and this box has three, two of them drawn as moves of the very border in question; and the `Scroller` rail's exemption rests on *a scroll region is identified by its content*, which an empty drop region has none of. The copy says WHAT, the boundary says WHERE, and only one of those is written down. **The hover has to move because `faint` INVERTS from the new rest** — 1.75 lighter in light, 1.19 in dark, the defect `field-border.dc.html` T4-3 refused `muted` over — and it lands on `muted` rather than `ink` because `ink` is the drag-over arm's own colour and taking it would collapse two states into one border; fields need `ink` only because a field is focusable and its hover competes with an `ink` focus ring. Three rungs stay strictly ordered on both adjacencies in both themes. **Stated because it is the honest half:** in dark the hover STEP narrows 2.18 → 1.74, accepted because the hovered edge itself goes 1.51 → 6.87 and both states now clear the bar where neither did. **A consequence outside this file:** `field-border.dc.html:165` derives the fields' `ink` hover from a floor whose only member was this dropzone, so that floor is left with none — the fields' ruling stands, the sentence justifying it is superseded, and `field-border.test.ts` carries a copy that #88 corrects. **Supersedes the TOKEN ASSIGNMENT only, never a hex** (parchment already took every hex): seven drop-target boxes in `data-portability.dc.html`, of which five are the rest edge (`:196 :365 :471 :485 :533`), one the hover (`:441`) and one — `:455`, the drag-over — is **not** superseded, plus that file's `:84` and `:181-190` prose, `field-border.dc.html:165 :192 :369`, and `switch-border.dc.html:203` and `:484`, which enumerate `panel-border`'s readers and name the dropzone among them. Its re-runnable survey is **structural, not copy-based** — five inline properties together — because one of the seven carries no drop copy at all, and its last two clauses are load-bearing: without them it also sweeps the danger-zone sub-panel, three times. It draws **none** of its own. Verified by computation, the precedent #90 and #86 set: every printed ratio recomputed from `index.css`, the survey re-run over all 21 drawings, the self-count computed rather than counted. Supersedes no geometry, copy, motion or state machine, and **edits none of them**. |

## Rules

- **Idiom = the master reference's** (`design/Investment Tracker.dc.html`):
  `<x-dc>` wrapper, ALL styles inline, every exact color/size/spacing literal
  in the markup, mock values inline. No runtime script, no `_ds/`/`support.js`
  references — extensions are fully static and render coherently via `file://`.
- **Dashed-border chips are spec annotations** (state microlabels), never UI.
  Motion (D7) specs live in HTML comments.
- **Precedence (D14):** once merged, an extension wins visual disputes for its
  surfaces; its brief wins copy/behavior disputes. Don't rework a merged
  surface in place — supersede via a new brief section + a new reference.
- Every literal color maps to an existing `src/index.css` `@theme` token or to
  a token the file's header comment explicitly mints — no ad-hoc hex.
- Light theme only until Phase 5 defines the dark palette.
- Mock figures shown are D5 demo values (pinned — additive-only) or clearly
  annotated live-dataset/audit illustrations; the app always derives them.

## EVERY FILE HERE STILL SAYS `Kubushka`, AND THAT IS CORRECT

They were drawn before D42 renamed the product, and D14 makes a merged reference
immutable — so the drawings keep the old name and the app does not. Recorded
2026-08-18, because E4 closed "last identifiers and docs" without it and a
reader rebuilding a surface from a drawing would otherwise copy a dead brand.
Four distinct divergences, three of them stale and one that never existed:

| Drawn | Shipped |
|---|---|
| Sidebar wordmark `Kubushka` — master reference, `Tracker Options` (7 variants), `settings.dc.html` | `Quirenote` over the `INVEST TRACKER` microlabel (README §5) |
| Download named `kubushka-backup-<date>.json` | `quirenote-backup-<date>.json` (`useBackupDownload.ts`) |
| Rejection copy *"it has no `kubushka-backup` marker"* | *"it has no `quirenote-backup` marker"* — the marker WAS renamed. D42's table says it stays and its own E1 amendment, 47 lines later, reverses that. The amendment is the ruling |
| `kubushka-mirror.json` and the whole file-mirror row, 7 times in `data-portability.dc.html` | **Nothing.** `feat/file-mirror` was retired outright, so those cells draw a feature that does not exist |

**Do not "fix" the drawings.** This is the same shape as D54's fonts and D56's
capsules: the reference is the record of what was drawn, the divergence is
recorded rather than silent, and README §4/§5 is where shape and copy are read
from when the two disagree.
