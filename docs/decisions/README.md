# Decisions — index

Why the project is the way it is. Read the one-liner here, open the one file for
the decision you actually need — **`D<n>` is `D<n>.md`**, always.

**The rules of this log live in [`RULES.md`](RULES.md)** — how to append one, why
a decision is superseded and never rewritten, how these tables are generated, and
which entries to read before touching anything. This page is the index and nothing
else (D102).

## D1–D20 — v1

| # | Decision | Date |
|---|---|---|
<!-- decisions:rows range="1-20" -->
| [D1](D1.md) | Tech stack: use `package.json` as-is | 2026-07-27 |
| [D2](D2.md) | Persistence: Dexie.js on IndexedDB | 2026-07-27 |
| [D3](D3.md) | Personal pet project: no Jira | 2026-07-27 |
| [D4](D4.md) | Testing: vitest for pure logic only | 2026-07-27 |
| [D5](D5.md) | Reference-data reconciliation | 2026-07-27 |
| [D6](D6.md) | Personal git identity + GitHub remote | 2026-07-27 |
| [D7](D7.md) | Fluid, soft motion on every interaction | 2026-07-27 |
| [D8](D8.md) | `src/core/` pure domain layer, structured returns, one sign convention | 2026-07-28 |
| [D9](D9.md) | Dexie `meta` table + versioning policy | 2026-07-28 |
| [D10](D10.md) | Scoped D4 amendment: `fake-indexeddb` for repository tests | 2026-07-28 |
| [D11](D11.md) | Settings persist versioning | 2026-07-28 |
| [D12](D12.md) | Backup envelope v1: hand-rolled JSON + zod | 2026-07-28 |
| [D13](D13.md) | Formula model: dual metric families + ledger-reconciled cash | 2026-07-29 |
| [D14](D14.md) | Design-extension workflow: immutable originals + `design/extensions/` | 2026-07-29 |
| [D15](D15.md) | Deploy: Amplify Hosting manual-deploy app driven by GitHub Actions | 2026-07-29 |
| [D16](D16.md) | Dual datasets: two Dexie DBs, boot-time binding, demo-first | 2026-08-02 |
| [D17](D17.md) | Destructive clears: typed-name AlertDialog idiom + erase scope | 2026-08-02 |
| [D18](D18.md) | Metrics exposure: per-asset XIRR flow model + additive placements | 2026-08-02 |
| [D19](D19.md) | Inzhur feed policy: public bare GET, tolerant parse, last-good cache | 2026-08-04 |
| [D20](D20.md) | Fetch quotes: draft-only fill, provenance in the draft, live ref picker | 2026-08-04 |
<!-- /decisions:rows -->

## D21–D40 — the cloud direction

| # | Decision | Date |
|---|---|---|
<!-- decisions:rows range="21-40" -->
| [D21](D21.md) | Fixed-yield automation: accrual ghosts, coupon confirm, projection fallback | 2026-08-04 |
| [D22](D22.md) | Reminders: derived ids, self-expiring dismissals, one toast per open | 2026-08-04 |
| [D23](D23.md) | Coupon occurrences are derived from the grid, not from the pointer | 2026-08-04 |
| [D24](D24.md) | Import replaces, never merges: validate → diff → confirm → one rw transaction | 2026-08-04 |
| [D25](D25.md) | `neg-tint` widened to irreversible-harm framing at block scale | 2026-08-04 |
| [D26](D26.md) | A price archive exists; the app is still local | 2026-08-11 |
| [D27](D27.md) | No single source of truth for prices; the axis is backfillability | 2026-08-11 |
| [D28](D28.md) | The capture journal, and how a frozen feed is caught | 2026-08-11 |
| [D29](D29.md) | CSV ships export-only; the round trip is cancelled | 2026-08-11 |
| [D30](D30.md) | **The observation key**: basis vocabulary, FX placement, instrument ref | 2026-08-11 |
| [D31](D31.md) | What one payload proves: fund basis, the FX channel, matured bonds | 2026-08-11 |
| [D32](D32.md) | Auth: Cognito Essentials, managed login, open registration | 2026-08-11 |
| [D33](D33.md) | There is no past-date prefill; value is derived | 2026-08-11 |
| [D34](D34.md) | The seed is rewritten to reconcile, not replaced | 2026-08-11 |
| [D35](D35.md) | Round 3 stays derived, and the small Round 4 items | 2026-08-11 |
| [D36](D36.md) | Three sign-in methods, one account per email | 2026-08-11 |
| [D37](D37.md) | Watching the 10,000 MAU free tier: what exists and what does not | 2026-08-11 |
| [D38](D38.md) | Registration is an application, not an open door | 2026-08-11 |
| [D39](D39.md) | Applications never touch Cognito; onboarding is passkey-first; SES lands with W7 | 2026-08-11 |
| [D40](D40.md) | The domain is `quirenote.com` | 2026-08-11 |
<!-- /decisions:rows -->

## D41 onward — rename, alerting, durability, observations, FX, radii, the shells, production, the window

| # | Decision | Date |
|---|---|---|
<!-- decisions:rows range="41-" -->
| [D41](D41.md) | ~~The product is Quirenote; the machines stay Kubushka~~ — **superseded by D42** | 2026-08-11 |
| [D42](D42.md) | The rename goes all the way, and now is when it is cheap | 2026-08-11 |
| [D43](D43.md) | Backfill fails on every historical date, and it is **not** the layout | 2026-08-11 |
| [D43 *(original)*](D43.md) | ~~The NBU parser only reads the current file layout~~ — **kept as the record of a wrong diagnosis** | 2026-08-11 |
| [D44](D44.md) | The alerting was silently dead, and every indicator said it was fine | 2026-08-11 |
| [D45](D45.md) | Alerts go to the Console Mobile App, **not** to email | 2026-08-11 |
| [D46](D46.md) | E3 is done: what the stack move actually cost and found | 2026-08-11 |
| [D47](D47.md) | The alert channel measures itself, and SNS is gone | 2026-08-11 |
| [D48](D48.md) | The payload split is not needed; the missing index was — **D90** opens a question against it, **D91** measures the answer: the index was right and could serve the ORDER BY backwards, but the query's own `to_char(...) AS as_of` alias hid the column and forced a table scan. Its "`payload_gzip` is never read" holds on index paths and fails on a table scan | 2026-08-11 |
| [D49](D49.md) | The DSQL durability gate passes, and the archive had no backup at all | 2026-08-11 |
| [D50](D50.md) | The NBU archive becomes observations, scoped narrow on purpose | 2026-08-11 |
| [D51](D51.md) | The NBU rate is fetched on request, and every failure is an HTTP 200 | 2026-08-12 |
| [D52](D52.md) | A price cannot tell you both when it was struck and at what yield | 2026-08-12 |
| [D53](D53.md) | What the xhigh review found, and the two fixes that were wrong first | 2026-08-12 |
| [D54](D54.md) | The brand fonts cannot write the app's default language | 2026-08-12 |
| [D55](D55.md) | The display face is chosen on figures, not on cap-height | 2026-08-12 |
| [D56](D56.md) | Nested radii are concentric, standalone radii are proportional | 2026-08-12 |
| [D57](D57.md) | The theme is one list of values, stamped as an answer | 2026-08-13 |
| [D58](D58.md) | Formatting follows the language, and the type system cannot see most of it | 2026-08-14 |
| [D59](D59.md) | Production is a branch, and the domain is what makes that real | 2026-08-14 |
| [D60](D60.md) | The repository is public, writable by one account, and rewritable by none | 2026-08-14 |
| [D61](D61.md) | Production sits behind Cloudflare's edge; the records that must not be proxied are named | 2026-08-14 |
| [D62](D62.md) | SMS is not a way around the SES denial; it trades one queue for two at 1,615× the price | 2026-08-14 |
| [D63](D63.md) | The environment split stops at the user's data; the archive has none | 2026-08-14 |
| [D64](D64.md) | The capture retries by firing again, not by waiting longer — **its guard DPU figure is superseded by D90** (~73/month, not ~6); its 0.3%-of-allowance figure was overtaken by D90, whose 1.6% D91 in turn supersedes. **No current replacement**: 0.3% is a year-1 projection and D91's 0.17% is a measurement of a much smaller archive — not comparable. The ruling stands throughout | 2026-08-14 |
| [D65](D65.md) | Scrolling is a drawn surface, and its gutter is the parent's padding, not the child's | 2026-08-17 |
| [D66](D66.md) | Two shells, one composition; and 44 px is hit area, never geometry | 2026-08-17 |
| [D67](D67.md) | Production moves on a version, not on a calendar — supersedes D59's cadence | 2026-08-17 |
| [D68](D68.md) | `muted` is re-derived against the surface it is worst on, and `label` is retired into it | 2026-08-17 |
| [D69](D69.md) | The provider's FX rate is not stored at all — supersedes D30's placement of it | 2026-08-17 |
| [D70](D70.md) | Capture checks are structural; the value check is retired — supersedes D28's frozen-feed half | 2026-08-18 |
| [D71](D71.md) | `as_of` is per source; the Inzhur rows were a day early and were migrated | 2026-08-18 |
| [D72](D72.md) | "the provider publishes no price history" is too strong — narrows D27 | 2026-08-18 |
| [D73](D73.md) | A branch always; there is no diff small enough to commit straight onto `dev` | 2026-08-18 |
| [D74](D74.md) | The funds' `nav` history is archived and never shown; the read-time conversion is rejected — closes O21 | 2026-08-18 |
| [D75](D75.md) | A hand-entered value is marked, an archive one is not — closes O22 by dissolving it into D33 | 2026-08-19 |
| [D76](D76.md) | Every branch is code-reviewed before it is squash-merged into `dev` — **amends D73's "no gate"** | 2026-08-19 |
| [D77](D77.md) | A merged reference wins the LAYOUT, not the class list — the reference owns the RESULT, the code owns the mechanism; three divergences on `/transactions` recorded, and the one that was NOT allowed named | 2026-08-20 |
| [D78](D78.md) | A window's opening position is valued the DAY BEFORE it opens — the boundary at which each transaction is counted exactly once, and the one that makes the full history reduce exactly; the extension's figures are off by one boundary day (37,00 ₴ on the seed) | 2026-08-21 |
| [D79](D79.md) | A derived schedule ASKS the walkers (`rollNextCoupon`, `nextUnsettledCoupon`) and never re-derives the grid — A41's first cut broke where the roll clamps at maturity (losing …6475's final травень coupon) and ended by the calendar where the app ends by settlement | 2026-08-24 |
| [D80](D80.md) | The window DOES change `Річна`'s basis (superseding Phase 6's pinned trap fix, owner's ruling on O24) — and F-3's grey ships with it: a row whose holding falls >10 % short of its basis renders `Річна` and `проти очікуваної` in muted, in every window including the default | 2026-08-24 |
| [D81](D81.md) | Every `/seasonality` insight card that summarises the bars reads the windowed ledger; only the `nextCoupon` half of «Купонний сезон» is a genuine forecast — a derived statement inherits the classification of the DATA it comes from, not of the sentence it is written as (A42) | 2026-08-24 |
| [D82](D82.md) | SMIDA's open-data API is alive and always was — D27's "retired 2021-06-30 (verified: 404)" recorded the wrong host: that date is where `stockmarket.gov.ua` stops, and its endpoints still answer `200` over frozen data. D27's ruling is untouched; only the availability sentence changes | 2026-08-24 |
| [D83](D83.md) | The provider's price files may be fetched automatically, superseding D72's by-hand rule (owner's ruling) — they are linked from the allowed offer pages and served from a CDN origin that publishes no `robots.txt`; `/documents` stays off-limits, and the filename carries a content hash so the link is re-read rather than polled | 2026-08-24 |
| [D84](D84.md) | Prettier is SCOPED, not retired — the 245 failures were an unset `endOfLine` on a CRLF checkout plus a default 80-char width against a 104-char codebase; `design/` (D14), all Markdown and the captured fixtures are ignored, 110 files reformatted, `format:check` joins BOTH CI gates | 2026-08-24 |
| [D85](D85.md) | `Річна` keeps ONE span shared by every asset rather than a per-asset one (the span itself is the window's since D80) — closing O23: the whole question rested on one young position, per-asset would have a fixed-coupon bond beating its own contract by 19,3 pp, XIRR already is the per-asset column, and D80's grey removed the silence that was the case for switching | 2026-08-24 |
| [D86](D86.md) | SMIDA's open-data API is NOT fetched by our code — categorically, closing O25 without the email: the file was ABSENT for sixteen months and returned in Nov 2021 carrying a `/db/` rule, so the restriction postdates the API and intent is on the record; the statute licenses use with attribution; the current file is an unambiguous blanket `Disallow` | 2026-08-24 |
| [D87](D87.md) | The number grammar FOLLOWS THE LANGUAGE, closing O26 — uk groups on whitespace and takes both `,` and `.` as the decimal, en keeps the comma as grouping; the both-marks rule is unchanged, `GROUPED_INTEGER` becomes English-only, every field groups live, and every in-flight string is re-formatted on a language switch. **Supersedes D58's one-parser half** | 2026-08-25 |
| [D88](D88.md) | **`/` and `/transactions` are composed like `/payouts`** — main's own width, `grid-cols-[1.6fr_1fr]`, `gap-3.5`, one column below `lg`, and `/transactions` mirrored so the ledger leads. **Supersedes the composition halves of two merged drawings** (screen-density § S1-B's centred 944, where-things-live § S4's form-on-the-left): the owner's own reason — both screens read as a different product from the rest of the app — and D14 was never meant to bind the person the drawing is for. Their findings stand; only the geometry framing them changed. Priced: the void returns to 238,9 from 115,8, and a container query left without a container evaluates false in silence | 2026-08-25 |
| [D89](D89.md) | **The backup vault takes a GOVERNANCE lock whose floor is read from the live plan** — found by a routine AWS sweep that was green everywhere else while the vault read `Locked: false`, one command from nothing (the **seventh** instance of the pattern below; D91 is the eighth). Governance not compliance because compliance cannot be lifted by anyone including AWS and the schema is still moving (W3/W4); a floor and no `max` because what is worth forbidding is retention being WEAKENED, and the floor is derived from the live plan because the script only ever CREATES one — a literal would silently reject every nightly job. It does NOT stop the human admin, it turns a one-command accident into a deliberate two-step act, and it also freezes existing lifecycles. The mode is the PRESENCE of `--changeable-for-days`, not an argument. **Proved by attempting a delete** — refused with `InvalidRequestException`, 15 points intact | 2026-08-25 |
| [D90](D90.md) | **What a week of real DPU corrected in D64, and the question it left open against D48** — the `alreadySettled` guard costs ~73 DPU/month not ~6 (its lookup reads 117 KiB, not the 2 KiB minimum the estimate assumed), and the workload uses ~1.6% of the free tier not 0.3%. **D64's ruling is untouched** — a supporting estimate was wrong, not the conclusion. Open question: a capture reads 34.2 MiB against 34.9 MiB stored, **answered by D91** (which also supersedes the allowance share: 0.17% today) | 2026-08-25 |
| [D91](D91.md) | **One aliased column cost ~64 DPU a night** — `to_char(as_of,…) AS as_of` made `ORDER BY as_of` bind to the TEXT output, not the indexed column, so the sort could not inherit index order, `LIMIT 60` bounded nothing and the planner scanned the table. Measured: **64.989 DPU verbatim → 9.508 with the column named**, against 0.559 on the 21-row `inzhur` branch and **1.210 for `count(*)` over all 6,664 rows**. Scanning the index is cheap; scanning the table bills `payload_gzip`. D48's index was capable and never given the chance. Cost now **~173 DPU/month = 0.17%**. `EXPLAIN (ANALYZE, VERBOSE)` prints `Statement DPU Estimate` — use it | 2026-08-25 |
| [D92](D92.md) | **Cross-browser beats offline, and the PWA shell leaves W7** — an owner priority ruling, recorded because it outlives the one deletion it caused. The spec's PWA row was already "installable shell, network-required, no offline", so the service worker vite-plugin-pwa registers was buying only installability at the price of the stack's most browser-divergent machinery. W7's remaining scope shrinks by one item; a bare-manifest install (no service worker) stays available as its own future item. **Supersedes the spec's `PWA \| vite-plugin-pwa` row and the "PWA shell" item in its Phase 3 scope, both annotated in place.** Not decided here: the derivation boundary the same conversation opened (filed as PLAN-OPEN **O28**, questioning the spec's `Derivation` row) and bare-manifest installability (**O29**) | 2026-08-25 |
| [D93](D93.md) | **The ledger's width cap comes off — inside a grid track, the track is the bound** (owner ruling, `/transactions`). `max-w-[884px]` in D88's `1.6fr` track protected nothing and opened a dead strip between the columns; the card now fills its track. Recorded because the cap had already been removed and argued back once — comments, `transactions-layout.test.ts` and `navigation-map.md` all updated in the same change, and the test now pins the cap's ABSENCE. The wide-monitor row stretch is accepted; a row-readability fix, if ever wanted, belongs to the row, not to a card un-filling its track. The form's 560 cap survives as a CONTENT measure — its own in-track binding above ~1800 viewport is priced in the entry; **the form half is superseded by D94 the same day** | 2026-08-25 |
| [D94](D94.md) | **The form cap is scoped to the stacked column** — the call D93 priced and left to the owner, made the same day: `max-w-[560px]` becomes `max-lg:max-w-[560px]`, so beside each other EACH card's own track is the bound (D93's rule, no exception left) and the 560 guards only the stacked column below `lg`, where a full-row form reads as a settings page. Supersedes the form half of D93's "Untouched on purpose" (annotated there); the test pins each card's exact width-token list, collapse prefix read from `/payouts`' row | 2026-08-25 |
| [D95](D95.md) | **No documentation file exceeds 200 lines** — the three plans and `BUILD-PLAN.md` split the way this log did in August: the named file stays put as the index, bodies move verbatim into ID-range files, closed work leaves for `../archive/`, IDs never change. `PLAN-NOW.md` went 2,211 → 58 lines, and `src/docs-line-cap.test.ts` fails the suite if any Markdown file crosses 200 | 2026-08-26 |
| [D96](D96.md) | **One file per decision; the range files are retired** — the append rule and the 200-line cap could not compose, so the bucket they disagreed about is gone. `D<n>` is `D<n>.md` | 2026-08-26 |
| [D97](D97.md) | **A50's re-plan: the alias changed a sort node and nothing else** — measured across nine range widths, BOTH forms pick the same access path (`Index Scan price_capture_as_of` to 1500d, `Full Scan` from 2000d), so the fix is `Sort` → `Incremental Sort` and no more. It does settle that DSQL serves a mixed-direction order from an ASC/ASC index. Warm 7-day cost is indistinguishable between forms (0.25594 against 0.25599 DPU, Read 99.3% of it). **D91's 0.356 DPU is UNREPRODUCED** — 0.26528 warm on D91's own window, and neither warmup, window content nor form accounts for the gap; **64.979 is one cold sample from the same session and untested here**. Method: one `EXPLAIN (ANALYZE)` is not a measurement, and a plan you did not run is not evidence — three of the four rounds inferred, and review caught each | 2026-08-26 |
| [D98](D98.md) | **The cap is a diagnostic, not a wall, and live plans split by section, not ID range** — both from the verifiable-documentation design's §6. `distillation-baseline.json` gains a `docLineCounts` section pinning only a Markdown file already over 200 lines; a file at or under it is unconstrained, and growth past a pinned length is diagnosed, not blocked. `A01-A20.md`/`A41-A50.md`/`A51-A60.md`/`W02-W08.md`/`W09-W17.md`/`O05-O29.md` become `section-c.md`/`section-m.md`/`section-p.md`/`phase-w-i-ii-iii.md`/`phase-w-iv-v.md`/`still-open.md` — D96's argument for decisions, restated for plans | 2026-08-27 |
| [D99](D99.md) | **W7's pre-condition is answered, and the answer is no** — DSQL rejects `USING btree` (`0A000 USING not supported for CREATE INDEX`) with and without `ASYNC`, and rejects a plain `CREATE INDEX` without `ASYNC` (`0A000 unsupported mode`). So promotion is TWO transformations of every index line, not one: insert `ASYNC`, strip `USING btree`. `USING` itself is not banned — `USING btree_index` is accepted and is what `pg_get_indexdef` prints, so the docs' silent grammar predicted the outcome with the wrong cause. Measured with it: all seven statements of `003_user_schema.sql` apply to the live cluster, its `CHECK`/`UNIQUE`/`DEFAULT` are enforced, `sys.wait_for_job` is a `CALL` and an accepted `CREATE INDEX ASYNC` is not yet a built index, and **DSQL now has enforced composite foreign keys**, which the drafts README stated it does not. Adding this entry also removed a 200-line wall in `pnpm decisions` that D98 had already retired everywhere else — O35 files where the index's growth should go | 2026-08-27 |
| [D100](D100.md) | **A constraint CAN be added to an existing DSQL table — as `NOT VALID`**, which enforces every new row, never checks the rows already there, and can never be promoted (`VALIDATE CONSTRAINT` is refused, so `convalidated` stays false for life). Bare `ADD CONSTRAINT` is refused, which is what made an earlier draft of this entry claim no constraint could ever be added — wrong, and corrected before it left the branch. Genuinely create-time-only, each probed in every spelling: `NOT NULL` (droppable, never addable), column TYPE, `UNIQUE` *as a constraint*, `PRIMARY KEY` (refused plain AND `NOT VALID` — the fact D30 rests on, now measured), and a `GENERATED … STORED` column. **`DEFAULT` is NOT among them** — a first draft said it was, from one spelling; `ALTER COLUMN … SET DEFAULT` is supported and applies to rows inserted after it. **So O34 is NOT dated** — and drizzle emits foreign keys as post-hoc `ALTER TABLE ADD CONSTRAINT` anyway, so one would be `NOT VALID` whether declared at W7 or years later. Promotion would need a third rewrite rule: append `NOT VALID` | 2026-08-28 |
| [D101](D101.md) | **O34 ruled: W7 ships NO foreign keys.** Owner's ruling 2026-08-28 — do not declare them at promotion, defer to O33. Grounds: D100 removed the deadline (a key can be added later as `NOT VALID`) and removed the advantage of earliness too (drizzle emits foreign keys as post-hoc `ALTER TABLE`, so one is `NOT VALID` at W7 as well); AWS's own `CREATE TABLE` guidance prefers `NO ACTION`/`RESTRICT` where child-row cardinality is unbounded, which transactions-per-user are; `RESTRICT` would force `repository.ts`'s `deleteAsset` to reverse its delete order; and `CASCADE` would answer O33 as a side effect instead of on its merits. **Whether they are ever adopted moves into O33**, which now decides cascade-vs-tombstone knowing real keys are on the table. Cost of waiting, stated: rows present when a key is finally added are never validated, so adopt only behind a clean integrity audit | 2026-08-28 |
| [D102](D102.md) | **O35 ruled: the log's rules move to `docs/decisions/RULES.md`, and the length cap counts AUTHORED lines only.** Owner's ruling 2026-08-28. `README.md` becomes the index and nothing else: a title, a pointer, and three tables that grow one row per decision. Its AUTHORED length is a couple of dozen lines and does not move when a decision is added — no absolute is quoted here, because this entry's own row would have aged it within the hour. `src/distillation/doc-line-counts.ts` gains `authoredLines`, which subtracts everything a generator wrote between its own markers (code-fenced examples excluded via `fences.ts`'s `inCode`), and both the ratchet and `pnpm decisions` ask that question instead. Exempting the FILE was the obvious reading and the wrong one — it turns the guard off for the prose too. Reverts D99's two-command rule: adding a decision is `pnpm decisions`, once | 2026-08-28 |
| [D103](D103.md) | **The inbox leaves the repository for GitHub Issues; the plans do not.** Owner's ruling 2026-08-28, on the criterion he named himself — filing an idea from a phone without extra movements, and being able to look at the list. `USER-FEATURES-DRAFT.md` and `USER-BUGS-DRAFT.md` are retired and their 27 lines are issues #1–#27, verified byte-identical including the U+00A0 in #1. Everything else stays in `git`: plans, decisions, `FOLLOW-UPS.md`. Measured grounds — 57% of plan commits change code in the same commit, 139 machine-checked claims live inside plan files, and D76 reviews documentation by diff, none of which survives a hosted tracker; the two draft files carried 3 of those 139 claims, no fact fences and no line-cap entry, because they were already exempt from every mechanism governing the rest. Grooming is the handoff and `Closes #N` in the squash-merge closes the issue, `dev` being the default branch. The ≤200-line exclusion in `doc-line-counts.ts` STAYS although the files that motivated it are gone | 2026-08-28 |
| [D104](D104.md) | **Two rulings on what the repository uses GitHub for: the wiki is OFF and Dependabot is security-only.** Owner's rulings 2026-08-28. (1) The **wiki is OFF** — never initialised, and the two mechanisms that hold this documentation honest cannot reach it: `pnpm test` (claim ratchet, line cap, decisions index, fact fences) and D76's review before a change lands. Deliberately NOT claimed, because both are false here: that a wiki lacks pull requests (so does this repository, D73) and that it lacks `format:check` (`.prettierignore` excludes `*.md` at every depth). CI is measured rather than assumed — neither workflow runs on a pull request, so the suite gates the DEPLOY, not the landing. (2) **Dependabot is security-only** — alerts plus automated fixes as repo settings, deliberately no `.github/dependabot.yml` (now asserted by `src/dependabot-config.test.ts`, because GitHub's UI commits that file when version updates are switched on), across **two manifests plus the Actions pins**: root pnpm, `infra/` npm, and the `@vN`-pinned workflow actions, which have no manifest at all. **The alert is the unit, not the PR.** It narrows D73's branch naming and D76's "no PR to hold" premise, which is why both are in `amends`. Also records the fourteen GitHub Releases created from the existing annotated tags. Three same-day rulings were split out as `D105` (work order), `D106` (dated gates) and `D108` (Issues views, Project, workflows), the last when this entry sat at the 200-line cap with no room for a correction | 2026-08-28 |
| [D105](D105.md) | **The live plans are finished first; issues are then worked one at a time, and none of them grows a task body in `docs/plans`.** Owner's ruling 2026-08-28, given in the session that wrote it down; it gets an entry rather than living only in `docs/plans/README.md`, which is rewritable. Finish first: **A53, A54, A46**; the five questions waiting on a ruling — **O27, O28, O31, O32, O33**; and **every live W item except W9–W12** — W3–W8, W13, W14, W15, W16, W17, Phase 6's chart analytics and Phase 7's DB browser included. **"All the current plans" is literal, confirmed by the owner when this entry's first draft tried to exclude the two big ones.** **The excluded set is four NAMED items — W9, W10, W11, W12** — whose gates ran 2027-01-01 to 2028-09-27, four months to two years past the ruling. They are named rather than described by a cut-off on purpose: "2027 or later" excludes every W item once 2027 arrives, and "a year or more away" would have included W9 on the day. A dated entry that is never rewritten cannot carry a moving threshold. Then each issue on its own branch, closed by `Closes #N`, with `bug` the one class allowed to preempt the order. **This narrows D103**, whose grooming clause makes an issue into work by giving it a Status row and a body in a section file: that path stays available, it is no longer the default, and the reason is the owner's — draining the inbox must not silt up the repository it was moved out of | 2026-08-28 |
| [D106](D106.md) | **Nothing automated watches `PLAN-WAITING.md`'s dated gates, and nothing should** — closing the 'a reminder is the candidate' line D103 left open. Owner's ruling 2026-08-28, split out of D104 when a draft tripped the 200-line diagnostic and its question — does this file hold a second purpose? — answered yes (the cap reports, it does not compel, D98). The mechanism is the table's own first rule, check it first every session; sessions landed on 21 of the 33 days from the first commit to 2026-08-28, longest gap 6. Most gates OPEN rather than expire, so arriving late costs waiting. **The `Hard?` column is the authority and names four — W5, W10, W11, W12** — of which W10/W11/W12 are 7, 11 and 25 months out. **W5's capture is automated by A6 so nobody need be present on the day, but its verdict is not: three unticked boxes ending in a DECISIONS entry, cost of missing 182 days**, and `UnexplainedQuoteAlarm` is not a detector for a wrong convention — it fires only when no yield can explain a quote, so a wrong-but-fittable convention is silent. **Carries a correction:** the first draft justified this with 'every date left is inside four weeks', which was false — the table runs to 2028-09-27 — and with 'gates never expire', which W11 contradicts | 2026-08-28 |
| [D107](D107.md) | **Rebase stays enabled on GitHub; the rule was always about merge COMMITS.** Owner's ruling 2026-08-28, after `allow_rebase_merge` was turned off and back on within the hour. A code review argued that D73's *arrives by squash-merge* was guarded by prose alone while the button sat on the page, and proposed disabling it; the setting was changed on a misread of the owner's intent and reverted the same day on his correction: **he forbids the ordinary merge, so that history stays linear without merge commits — rebasing is allowed.** Settings now: `allow_merge_commit: false` (enforced), `allow_rebase_merge: true`, `allow_squash_merge: true`. **Squash remains the convention** for how a change arrives on `dev` (D73), and it is a convention, not a setting. Recorded because it lived only in `CLAUDE.md`, which is rewritable, and because the same proposal will otherwise be made again by the next review that measures the setting against the rule | 2026-08-28 |
| [D108](D108.md) | **Two saved Issues views, a GitHub Project ahead of need, and the workflow that must stay OFF.** Owner's rulings 2026-08-28, split out of D104 when that entry sat at the length cap with three subjects and no room for a future correction. `Needs sequencing` (`no:milestone`) is load-bearing: an issue filed from the GitHub app arrives with no milestone and would otherwise land invisibly among the sequenced ones. `Bugs` is the `bug` label — the one class D105 lets preempt the plan order, so labelling is a queue decision, not a tone. The **Project was created knowing it buys nothing until issues start moving**, so the board is not configured in a hurry mid-flow; it holds **ISSUES ONLY**, never a plan task, enforced by narrowing the auto-add filter to `is:issue`. **`Auto-close issue` is OFF** because closing must come from `Closes #N` in the squash-merge, not from dragging a card, or the commit↔issue link that justified choosing GitHub Issues is lost. **No workflow sets the milestone and none should** — a Projects workflow cannot (Milestone is a mirrored field), and an Actions one would empty `Needs sequencing` for good and delete the only triage point | 2026-08-28 |
| [D109](D109.md) | **A gate whose verdict depends on what else is running is not a gate — nested checkouts are now invisible to every tool that walks the tree.** A background agent's worktree lands at `.claude/worktrees/<name>/`, a SECOND checkout of this repository inside it, and four of five repo-walking tools followed it in. Measured with two worktrees open: `git add -A` staged two **embedded repositories** into the index, **eslint linted 456 files of 685** (two thirds of the run), **vitest collected 225 test files against a real 75**, and prettier turned `format:check` RED on a fixture `.prettierignore` exempts in the real tree. Only `src/facts/markdown-files.ts` was right, its `SKIP` set having held `.claude` all along. The `.gitignore` rule is **`**/.claude/*` with the four SHARED entries negated back in** (`settings.json`, `agents/`, `commands/`, `skills/`): contents excluded rather than the directory, because git cannot re-include a file whose parent DIRECTORY is excluded, so a bare `.claude/` silently defeats every negation; `**/` because a pattern containing a slash is ROOT-ANCHORED and would leak `infra/.claude/…`; and the negations root-anchored in turn, since a nested `.claude/` is pure session state with no shared half to rescue. **The tools are then deliberately asymmetric** — git commits the shared four, while eslint and vitest exclude `.claude` WHOLE, a vendored skill being configuration rather than this repository's source (measured: a `*.test.ts` under `.claude/skills/` was collected into `pnpm test`), and **`.prettierignore` takes `.claude/` whole** — the four negated entries are not gitignored, so prettier reads them and is the only gate that does: `/update-config` writes `settings.json` without a trailing newline and a skill ships `scripts/*.js`, `references/*.json` and `*.yaml`, four files RED. Narrowing that entry to `settings.json` was tried and covered one of them. **Prettier rides on `.gitignore`** — which covers the scratch for free, plus explicit exclusions in `vitest.config.ts` and `eslint.config.js`, **neither of which reads it**. Guarded in ONE file, `src/nested-checkouts.test.ts`, because it is one bug with four surfaces and fixing three reads as fixed; three of the four are asked BEHAVIOURALLY (`git check-ignore`, `prettier.getFileInfo`, `ESLint#isPathIgnored`) rather than by grepping their config, each with a negative anchor and each probed OUTSIDE the worktrees at a **`.ts`** path — the extension is load-bearing, since `isPathIgnored` reports "ignored" for any path no `files` pattern claims, which made a `.json` probe vacuous. Vitest is the exception and is asked structurally, no glob engine being resolvable at the root. **The cost was never the wasted work** — it is that `pnpm lint` and `pnpm test` answered a different question depending on whether an agent happened to be running, so any green run recorded while a worktree was open proved less than it appeared to | 2026-08-29 |
| [D110](D110.md) | **A Dependabot PR is merged LOCALLY, under the repository's own authorship — never with `gh pr merge`.** GitHub's squash preserves the PR author, so the button would land a `dependabot[bot]`-authored commit on `dev`; [`D104`](D104.md) carves out the branch NAME only, and `CLAUDE.md` pins authorship to `RomanKushyk <romankushyk0@gmail.com>`. Owner's ruling on the first such PR (#28, react-router 7.18.2). The link survives the change of mechanism: `Closes #<n>` in the commit body closes the PR because `dev` is the default branch — measured, the PR closed and the alert flipped to `fixed` on the first poll after the push. **Three further facts were measured on that PR and belong with it.** **pnpm 11 NO LONGER READS** the `pnpm` field in `package.json`, so the override recipe [`DEPENDABOT.md`](../reference/DEPENDABOT.md) prescribed was dead — it prints a `[WARN]`, succeeds, changes nothing, and passes every gate green, with `esbuild@0.18.20` still in the lockfile; overrides live in `pnpm-workspace.yaml`. The discriminator for a transitive advisory is **not** transitive-vs-direct but whether the PARENT'S DECLARED RANGE admits the fix — `pnpm update <pkg>` defaults to `--depth Infinity` and moved js-yaml 4.3.0 → 4.3.2 in four lockfile lines, so the runbook's 'it is a no-op' was false. And **`pnpm build` joins the local gate list**: it is the only one that resolves through Vite's production `exports` conditions, and **no workflow triggers on a `dependabot/*` branch at all**, so the local gates are not a second opinion, they are the only one | 2026-08-29 |
| [D111](D111.md) | **Public holidays are not a distinct calendar class while martial law stands, so the archive does not model them.** Owner's ruling, 2026-08-31, given while reading the W3 observation window: holidays in Ukraine are suspended under martial law, and the measurement agrees — NBU published a fair-value file on **24 August, Independence Day**, with 185 entries and a byte size sitting between its Friday and Tuesday neighbours. No holiday effect at all. **W3's holiday question is therefore closed as MOOT rather than answered**, which matters because the window could not have answered it anyway: 2026-08-24 fell on a **Monday**, the one weekday on which Inzhur fund NAV is already static, so the holiday effect and the Monday effect are inseparable in that observation and would be in any future one that lands the same way. The standing rule is: treat a public holiday as an ordinary calendar day for capture, completeness and alarm purposes. **Revisit when martial law ends** — that is the trigger, not a date, and nothing watches it (D106) | 2026-08-31 |
| [D112](D112.md) | **Units are a DERIVATION over the ledger, not a stored total — `units(a, D) = Σ quantity deltas`.** Issue #31 reported fetched quotes that "суттєво не співпадають"; measured, a position linked at 5 000 units that later reinvested ₴11 138.90 fetched 55 694.50 where it was worth 66 833.40 — **16.7% short, and widening with every later purchase**. Root cause: `Asset.inzhur.units` was one hand-typed number written only by the asset form, `Transaction` carried no units at all, and a stored `Snapshot.quotes[id]` is a POSITION VALUE rather than a per-unit price — so the correction could not be computed from anything the store held. **This is not new design: W7 already pinned it** — `infra/schema/user.ts` gives `transaction` a `quantity` and a `unit_price`, gives `asset` no units column at all, and states the model as `value(a, D) = units(a, D) × coalesce(user_price(a, D), archive(a, D))`. So the app's local model moves FORWARD to the target rather than inventing a third shape. `Transaction.quantity`/`unitPrice` are optional in both directions and stay that way — every row recorded before this carries neither and `w7-migration-translations.md` §4 says those counts are unrecoverable. **`Asset.inzhur.units` survives as a FALLBACK, and `InzhurMatch.unitsFrom` says which of the two answered** — it is the only seed for the backfill, so removing it before the ledger carries quantities would destroy the one number the migration needs. **Per-unit quotes are NOT part of this** — owner's ruling, 2026-08-31: `Snapshot.quotes` stays a position value until W7, because converting it means rewriting the seed, which `PLAN-OPEN` O31 gates | 2026-08-31 |
| [D113](D113.md) | **The backup envelope moves to `formatVersion: 2`, and a v1 file is no longer importable.** [`D112`](D112.md) adds `Transaction.quantity` / `unitPrice`, and the rows are `z.strictObject`, so an older build meets keys it has not declared and rejects the file with `unrecognized_keys` — naming a field where it should name a version. **The break is the OTHER direction and it is deliberate:** every v1 file on disk is now refused, and the owner accepted that on 2026-08-31, on the grounds that the platform has no live users and their own data can be deleted. This repository already ruled the identical question once — the `kubushka-backup` → `quirenote-backup` rename, which [`D42`](D42.md) had kept precisely so old exports stayed readable and which was reversed during E1 on the same grounds. **A third rejection code joins the two [`D24`](D24.md) pinned:** `older-format`, so a genuine backup from before the change stops sharing a sentence with a hand-edited `0`. **Supersedes:** D24's "`formatVersion` 1 is current" and its two-case version policy, and D12's `formatVersion: 1` envelope literal and its "a non-1 `formatVersion` is rejected". D12's forward-compatibility MECHANISM stands unchanged and is the reason the bump was needed at all: pre-declaring an optional field keeps the version still, and these two shipped in the same commit as their writer, so nothing pre-declared them | 2026-08-31 |
| [D114](D114.md) | **Every segmented control is FILLED: the track is the plane's foreground, the sliding chip its background.** Owner's rulings, 2026-09-01, after reporting the price-mode toggle's slide as "the animation stopped working" — measured, the thumb travelled 712.6 → 743.2 over one 300ms transition and nothing visible moved, because `card` on `panel` is **1.09:1** in dark. `card` cannot be the chip in both themes: it is LIGHTER than `panel` in light (#ffffff on #eceae7) and DARKER in dark (#1c1c1f on #232327), so a chip painted `card` reads as raised in one theme and as a recess in the other — the inversion [`D57`](D57.md)'s shadow section already names and compensates with a 1px ring, which is `panel-border`, the same value as the track's own border. **The idiom is `bg-ink` track + `bg-card` chip**, giving 14.16:1 in dark and 15.07:1 in light, and it inverts with the theme by itself. Text follows the surface it sits on: the active label stays `text-ink` against the chip, the idle label becomes `text-page` against the track. **The rail's currency toggle is the ONE exception, and it is a ruling** — owner, 2026-09-01, after seeing the filled version: `pillClass` paints the ACTIVE route `bg-sidebar-text` with `text-sidebar`, so a filled track made the UNSELECTED currency render exactly like a selected route twenty pixels under a list of them, while the selected one took the rail's own fill — an inactive pill's treatment. In that plane a light lozenge already means SELECTED, and one control cannot say otherwise. It keeps `sidebar-inset` track + `sidebar-text` chip. **A filled track needs its focus ring in the plane's BACKGROUND:** the base rule paints the ring `var(--color-ink)`, so on an `ink` track it sat at 1.00:1 on every segment of every filled control — `[data-filled-track] :focus-visible` sets `page`. **`--shadow-thumb` is now the Switch's alone**, and the intermediate `--color-segment-thumb` (#454550, a mid-grey chip at 1.66:1) is retired unused. **Supersedes** D57's reading that the segmented thumb is `card` on `panel` and that `--shadow-thumb` has four call sites | 2026-09-01 |
| [D115](D115.md) | **Geometry is measured in the chrome-devtools MCP, against a calibration probe — not in Playwright and not in a background tab.** Measured 2026-09-01: a probe styled `border: 1px solid red; outline: 2px; width: 100px` computes to **0.571px / 1.714px / 100px** in the Playwright MCP's headless Chromium and to **1px / 2px / 100px** in real Chrome, at `devicePixelRatio` 1, `zoom` 1 and root font-size 16 in both. Lengths are honest there; border and outline THICKNESSES are not — so every figure derived from a box that includes a border came out short, and `style/segmented-idiom` recorded a segment as 15.4 × 16.9 where it renders **15 × 16**, and a pitch of 19.4 where it is **19**, in a docblock, in [`D114`](D114.md) and in a WCAG comparison. **Three more ways a browser reading lies, all of them hit the same day:** an ANIMATED property sampled inside its transition (Tailwind's `transition` covers `outline-color`, and one focus ring read 1.00, 13.47 and 13.84 depending on when) — kill transitions before reading; a BACKGROUND tab, where `visibilityState` is `hidden`, transitions freeze at `currentTime` 0 and `animationend` never fires, which read as "the animation is broken" and as "the transform is not applied"; and a `data-theme` flip or an HMR update without a reload, which serves stale CSS while `getComputedStyle` reports it confidently. **The rule: check `document.visibilityState`, drop a calibration probe, disable transitions, and prefer a full reload over a live theme flip.** A number that disagrees with the arithmetic of its own classes is the instrument, not the code, until proven otherwise | 2026-09-01 |
| [D116](D116.md) | **The asset form asks two fewer questions: the Inzhur KIND is derived from the yield type, and the reinvest policy is a LIST.** Owner's design, 2026-08-31. **Kind:** the Fund/Bond segment never had an answer of its own — an Inzhur bond is an OVDP, which is `fixed_coupon`, and everything the provider lists that is not a bond is a fund, so the control could only agree with the yield type or contradict it, and contradicting it produced a picker listing the wrong instruments with no way to tell why. `kind = yieldType === 'fixed_coupon' ? 'bond' : 'fund'`, and turning the link on now adds ONE control. **The ref clears when the kind changes** — a fund slug is not an ISIN, so a stale ref could only fail to match, silently, at the next fetch. `KindSegment` is deleted; the geometry it defined now lives on `TransactionPanel`'s `PriceModeSegment`, the app's only remaining segmented control of that size. **Reinvest policy:** the owner reported the free-text field unused "через нерозуміння як ним користуватись", and the field's only FUNCTIONAL role was ever truthiness — `/allocation` tests it to append "or pause the reinvestment" to a sell action, `/attributes` displays it. So it becomes a Select over stable KEYS (`auto_dividends`/`auto_coupons`/`manual`) with localized labels, and **"no reinvestment" is the EMPTY STRING** — a non-empty "none" would have `/allocation` offer to pause a reinvestment the asset does not do. **`Asset.reinvestPolicy` stays `string`, not a union**, and a legacy value is carried as its own option: the field was an open input for the app's whole life, so real data holds real sentences, and a Select with no matching option renders empty — which reads as "no policy" and then saves that | 2026-08-31 |
| [D117](D117.md) | **The asset form's Units field is gone; `Asset.inzhur.units` is legacy, optional and never written again.** Owner, 2026-08-31, pointing at the quick-create flow where TWO «Одиниці» fields stood on one screen at once — the Inzhur group's and the transaction's own. In that flow the transaction being recorded IS the first purchase, so the group asked for a count the row beneath it already carried. Units are `Σ transaction.quantity` ([`D112`](D112.md)), so the field had nothing left to be. **The PROPERTY stays**, optional: it is the only unit count an asset linked before this date has — `w7-migration-translations.md` §4 calls those counts unrecoverable — so `matchAssets` still falls back to it and nothing regresses before the owner's hand backfill. **Three things had to change so the removal is not a silent deletion.** (1) `assetPatchFromForm` replaces `inzhur` WHOLESALE, so every edit of a linked asset would have dropped the stored count; it now carries it across, **but only while kind+ref are unchanged** (trimmed, case-folded — a count was counted for one instrument, and re-pointing the link would value the new holding at the old one's size). (2) `assetFormDefaults` deliberately does NOT round-trip the legacy value — it survives precisely because nothing writes it back. (3) `matchAssets` gained a THIRD state: ledger, legacy link, or **nobody knows** — the last yields a match with no `value`, and `reconcileFetched` skips it rather than filling a draft with `undefined` or with 0, which is a real answer and the one a sold-out position gives | 2026-08-31 |
| [D118](D118.md) | **The Reinvest policy control is removed — it was structurally unreachable, and [`D116`](D116.md)'s dropdown did not fix that.** Owner asked what function the select actually served; the answer, measured: the field was **editable only inside the fixed-coupon group** (`yieldType === 'fixed_coupon'`) and **rendered only in `Attributes`' NON-bond branch**. Mutually exclusive. On a bond you could set it and never see it; on a fund you could see it and never set it — the seed's REIT holds `auto_dividends` only because the seed wrote it directly, and `fixedCouponFields()` means a non-bond patch never touches the key at all. Its two consumers were a **truthiness test** on `/allocation` (append "or pause reinvest" to a SELL row — the four values were indistinguishable to it) and a **display string** on `/attributes`. **Nothing in `src/core/` read it; it drove no derivation and no automation.** `/portfolio`'s "· auto-reinvested" is NOT this field — it tests `reinvested[assetId] > 0`, real `reinvest` transactions, which is the only place the app actually knows about reinvestment. So D116's dropdown made a field convenient to fill that is not read where it is filled. **Removed:** the control, both renders, `reinvestPolicyOptions`/`Label`/`REINVEST_POLICIES`, the `/allocation` hint, and eight dictionary entries. **KEPT:** `Asset.reinvestPolicy?: string`, the DSQL `reinvest_policy` column, the CSV column and the JSON backup member — the seeded value survives precisely because NOTHING writes it now, and a real reinvestment feature has somewhere to land | 2026-08-31 |
| [D119](D119.md) | **The asset form asks for the coupon RATE, not the coupon amount — and the amount becomes a derivation.** Researched against a live `/_api/assets` on 2026-08-31: all **32 bonds** repay **₴1000 per unit** at maturity, each publishes **exactly one distinct coupon value** (which proves a fixed-amount-per-period convention, not a day count), and coupon dates sit **182 days apart** — 123 of 124 on a Wednesday. So `annualRatePct = perUnitCoupon × 2 / 1000 × 100 = perUnitCoupon ÷ 5`, exact to 2dp for all 32, range 9.79 %…18.50 %. Full measurement: [`OVDP-COUPON-STRUCTURE.md`](../reference/OVDP-COUPON-STRUCTURE.md). **`returnRates` is the YTM and NOT the coupon rate** — measured gap up to **4.30 pp, and it changes sign**. `Asset.couponRatePct` replaces `couponAmount` as what the form asks; `couponPerPayment(asset, units)` computes `rate/100 ÷ paymentsPerYear × 1000 × units`, so the figure now SCALES with the holding — the same defect issue #31 reported for `inzhur.units`. It agrees with the provider's schedule **by construction**, since `rate × 5` IS the published per-unit coupon, which closes a live inconsistency: the coupon card prefilled `feed × units` while the ghost accrual and `/overview`'s projection used the hand-typed absolute. **`couponAmount` stays as the legacy fallback** — the seed's two bonds have no quantities to scale a rate by. **`expectedPct` is NOT folded in**: on a bond it is YTM at purchase, a different quantity that depends on the price this holder paid | 2026-08-31 |
| [D120](D120.md) | **YTM at purchase is DERIVED, and the target field is renamed.** `derivedYtmPct` solves the yield the price this holder paid implies against the bond's own published schedule on `firstPurchase`, using `impliedYield` — the bisection inverse `dcf.ts` already shipped — over `purchaseUnitPrice`, the `unitPrice` of the earliest recorded purchase (#31 added it). Three inputs, each optional: no price on a purchase made before #31, no schedule on an unlinked bond, and the stored `expectedPct` shows whenever either is missing. **Never stored** — the premises are captured, the conclusion is not (D31): a stored YTM goes stale on a schedule revision with nothing to say so. The EARLIEST purchase, not an average, because "at purchase" names one moment. **`targetPct` was labelled «Ціль, %» / "Target, %" in the form and «Цільова частка» everywhere else** — it drives `/allocation`'s whole rebalance plan (`sharePct`, `allocationDeltaPp`, `trimAmount`, `topUpAmount`), so the form now says what it is. **Two further findings are recorded but NOT acted on**: `maturity` and `nextCoupon` are heavily load-bearing rather than inert — between them they drive the coupon walk, the payout projection, the seasonality bars, the maturity reminder and the accrual's stop — though both are derivable from the feed for a linked bond; and `payoutSchedule` cannot simply be bound to `yieldType`, but the form's default pairing (`fixed_coupon` + `maturity`) contradicts all 32 live bonds and both seed bonds, which are `semiannual` | 2026-08-31 |
| [D121](D121.md) | **Naming a bond fills its maturity, next coupon, payout cadence AND coupon rate from the provider's own schedule.** `scheduleFacts(quote, fromIso)` reads all three off one feed entry: `maturity` verbatim, `nextCoupon` from `nextPaymentOnOrAfter` (so it inherits the coupon-vs-principal tie-break), and the cadence from the MEDIAN gap between distinct payment dates, read in BANDS — 182 days is the measured norm but one of the 32 live bonds carries a 183/181 pair around a shifted date, and matching 182 exactly would misread it. **One payment means `maturity`** (a zero-coupon bond), and a cadence outside the bands returns **nothing** rather than guessing: an annual bond has no member in the enum, and `payoutSchedule` is the divisor in `couponPerPayment`, so a wrong guess would corrupt every coupon figure the asset produces. **Overwriting IS correct here, unlike a quote fetch (G5)** — these are facts about the instrument just named, and a maturity left over from a previously picked bond is the stale-ref problem [`D116`](D116.md) solved one field over. Keyed on the last applied ref, so it fires once per instrument and re-arms when the ref is cleared. **Also corrected: the form's default pairing was `fixed_coupon` + `maturity`** — a zero-coupon bond — while all 32 live bonds and both seed bonds pay `semiannual`. [`D120`](D120.md) found that; this fixes it | 2026-08-31 |
| [D122](D122.md) | **The backup envelope moves to `formatVersion: 3`, and a v2 file joins v1 as `older-format`.** [`D119`](D119.md) adds `Asset.couponRatePct`, and the rows are `z.strictObject`, so a build that does not declare the key refuses the file with `unrecognized_keys` — naming a field where it should name a version. That is the exact mechanism [`D113`](D113.md) bumped 1 → 2 for, applied to the field that shipped next. **The objection this answers is "2 was never promoted, so reuse it"** — true, `main` still serves a v1 build, so no production user has ever seen a v2 file, and 2 and 3 landed within days on the same line of work. Still wrong: `dev` deploys on every push ([`D67`](D67.md)), so a build carrying `quantity` / `unitPrice` but NOT `couponRatePct` was live on dev.quirenote.com between the two merges, and a backup taken from it is a real file someone can hold. Reusing 2 would give two deployed builds the same version number and different accepted fields — the single thing the number exists to prevent. **The version tracks what a build ACCEPTS, not how much time passed or how far the change travelled.** **Nothing in the mapping had to change:** `import.ts` compares against `BACKUP_FORMAT_VERSION` rather than a literal, so v2 became a named older format by arithmetic and the fractional and zero cases keep the honest catch-all. Only the pinned literals in the two test files moved, and they stay literals on purpose — a version bump should be a deliberate edit, not something a derived expression absorbs silently. **Supersedes** D113's version literal and its "BUMPED TO 2" docblock heading; D113's RULING is untouched and is the reason this bump exists | 2026-09-01 |
| [D123](D123.md) | **The seed's `reinvestPolicy` is the free-text `'Auto (dividends)'`, not `auto_dividends` — [`D116`](D116.md) and [`D118`](D118.md) both state otherwise and both are wrong.** D116 migrated the seed to a stable key because it introduced a Select over keys; [`D118`](D118.md) then removed that control, `reinvestPolicyOptions`, `reinvestPolicyLabel` and eight dictionary entries — and the seed migration went with them, correctly. **The SEED is right and the log is wrong, and the correction needs no further argument than that:** nothing in `src/` reads or writes `reinvestPolicy` any more — D118 removed BOTH renders along with the control — so the stored value is INERT and neither form of it is better for the app. `grep auto_dividends src/` returns nothing outside these decision files. (An earlier draft of this entry claimed `/attributes` still renders the field verbatim; it does not, and reaching for that argument is how a correction acquires its own error.) **Recorded rather than corrected in place**, per `RULES.md` — a decision is superseded, never rewritten, and both citations remain accurate about everything else they say. **What a reader needs from this:** `Asset.reinvestPolicy` is `string` or absent holding whatever was typed, D118's KEPT clause governs, and nothing in the app writes it. Anyone reasoning about a stable key from D116's table will not find one in the data. **Supersedes** D116's "now migrated to `auto_dividends`" and D118's "the seed's REIT holds `auto_dividends`"; neither ruling is otherwise affected | 2026-09-01 |
| [D124](D124.md) | **A position-moving transaction now REQUIRES its unit count — at the form door only.** Owner's ruling, 2026-09-01, answering the gap [`D119`](D119.md) opened: with every coupon figure derived as `rate × units`, a `buy` recorded in the panel's default `total` mode with the count left blank produced a bond whose coupon reads «—» on `/attributes`, drops out of `/seasonality`'s coupon season, falls back to an `expectedPct` ESTIMATE on `/overview` and prefills nothing in the due card — with nothing anywhere saying the rate had been ignored, or why. **[`D112`](D112.md) declined to enforce this and its reasoning was sound for its own scope but aimed at the wrong door:** every row recorded before #31 lacks a count and those counts are unrecoverable, so refusing them would make the app unable to READ its own history. True — and `transactionSchema` never reads history. It sees one row being typed now. **So the asymmetry is deliberate and load-bearing:** `transactionSchema` refuses a count-less `buy`/`sell`/`reinvest`/`redemption`, while `Transaction.quantity`, the JSON backup row and `transaction_quantity_absent_ck` all keep it optional — the DDL still constrains only the other direction, because a CHECK the other way would refuse every legacy row at migration. The rule is about what the app WRITES from today. **It subsumes the `priceMode === 'unit'` refinement** (the panel forces `total` on any type that takes no units, so `unit` implies a moving row), and it needed no new message: `quantityMissing` already existed for the per-unit case. **The demo seed is untouched and still has zero quantities across its seven moving rows** — it does not pass through this schema, and its bonds keep showing their legacy `couponAmount`, so no pinned figure moves. **Amends** D112's "optional in both directions and stay that way" | 2026-09-01 |
| [D125](D125.md) | **The unit count is required at ALL THREE doors, not just the form — and the backup envelope moves to `formatVersion: 4`.** Owner's ruling, 2026-09-01, extending [`D124`](D124.md) the same day. D124 enforced the count in `transactionSchema` and left storage optional; that was not enough, because **the form is not the app's only writer.** `CouponDueCard` builds a `reinvest` and hands it straight to `recordTransaction`, bypassing the schema entirely — so a count-less moving row could still reach the store and, from there, an export. **What changed:** the JSON backup refuses a position-moving row with no `quantity` (new issue code `units-missing-on-position-row`, discriminated from D112's opposite rule by a `params.rule` on the zod issue, so `core/` still emits paths and never English); `transaction_quantity_required_ck` is the DDL converse; and the coupon card grew a units field, revealed with its reinvest checkbox and required while it is on, because the card holds ₴ and no price and cannot derive a count without inventing one. **`unitPrice` deliberately keeps only the one-way rule** — it is derivable from `amount / quantity`, so a row without it loses nothing, while a row without the COUNT cannot be valued or scaled at all. **THE SEED GAINED SEVEN COUNTS** (6164 / 6000 / 15 / 4 / 0,2192 / 43,4835 / 61,6161): without them the app would export a demo backup it could not re-import. Measured after: all 1312 tests still passed, so no pinned figure moved — the seed's bonds carry `couponAmount` and no rate, so `couponPerPayment` never reaches the units. **The DDL check is a deliberate migration break:** D112 declined it because pre-#31 counts are unrecoverable, which turns from a permission into a SEQUENCING requirement — the W7 backfill must run before that table takes the owner's history. On D113's standing grounds: no live users, and their own data can be re-entered. **Version 4 is the first bump for a STRICTER READER rather than a new field**, and [`D122`](D122.md)'s rule covers it unchanged: the version tracks what a build ACCEPTS. **Amends** D124 (form-only enforcement), D122 (version literal 3) and D112 ("optional in both directions") | 2026-09-01 |
| [D126](D126.md) | **The unit count is required where data is CREATED and where it MIGRATES — not in the JSON backup.** Reverses [`D125`](D125.md)'s backup-door half, six hours later, because the seventh pre-merge review found it deadlocked the exact data it was meant to protect. **A BACKUP IS A MIRROR, NOT A DOOR:** its job is to carry what the store legitimately holds so it can be restored, and the store holds pre-#31 rows whose counts are unrecoverable. Requiring the count there meant a database created before the branch **could not export** (the export re-reads its own output), **could not import** (the pre-import safety backup is gated on that same export, and `ImportDialog` returns without importing when it fails), and **could not back up before a wipe** — while `useUpdateTransaction` has no caller anywhere in `src/`, so the app has no control for editing a historical row and the failure named a remedy that does not exist. Three ways out, one door: none. **What stands:** [`D124`](D124.md)'s form rule, the coupon card's own units field, and `transaction_quantity_required_ck` — whose backfill-before-migration sequencing was always the point and is unaffected. **`BACKUP_FORMAT_VERSION` goes back to 3**, because D122's rule cuts both ways: the version tracks what a build ACCEPTS, 4 existed only for the stricter reader, and nothing ever shipped it. **The export guard STAYS** — it is what turned a silent unrestorable file into a refusal at the moment of writing, which is how the deadlock became visible at all; it now checks the whole envelope and reports the parser's own first issue rather than guessing a cause, and honours `quiet` like the catch beside it. **Amends** D125 (its backup rule and version literal) | 2026-09-01 |
| [D127](D127.md) | **The unit count IS required in the JSON backup, and the pre-#31 deadlock is ACCEPTED.** Owner's ruling, 2026-09-01, reinstating [`D125`](D125.md)'s backup rule against [`D126`](D126.md), which had reversed it. **The consequence is unchanged and is now a known state, not an oversight:** a database created before this branch holds rows whose counts are unrecoverable, so it cannot be exported (the export re-reads its own output), cannot be imported into (the pre-import safety backup IS that export), and cannot be backed up before a wipe — and the app has no control for editing a historical row, `useUpdateTransaction` having no caller in `src/`. The concern was raised with that consequence spelled out and **the ruling was reaffirmed after it**, which is what makes this a decision rather than a defect. `BACKUP_FORMAT_VERSION` returns to **4** on D122's rule — the version tracks what a build ACCEPTS, and this one refuses a count-less `buy`. **What D126 got right and is kept:** the export guard, which turned a silent unrestorable file into a refusal at the moment of writing and is what made the deadlock visible at all. **One wording change survives the reinstatement:** the failure no longer names the Transactions screen as the remedy, because no edit control exists there — it states how many rows and stops. **The way out is a transaction-edit affordance**, which is a feature this branch does not contain. **Supersedes** D126 entirely | 2026-09-01 |
| [D128](D128.md) | **There is no pre-#31 database, so the deadlock [`D127`](D127.md) accepted does not exist — and the code handling it is gone.** Owner's observation, 2026-09-01: with no live users there is no live data, therefore no lower-version backup and no stored row without a unit count. Checked against every door that can put a position-moving row into the store, and all four are closed: the seed carries seven counts of its own ([`D125`](D125.md)), `transactionSchema` requires one ([`D124`](D124.md)), the coupon card asks for one, and the importer refuses a row without one (D125, reaffirmed by D127). **A count-less row can therefore exist in exactly one place: a database seeded BEFORE this branch** — which for this project is disposable local state, cleared and re-seeded rather than migrated. **What goes:** the export guard's missing-units counting branch and both `missingUnitsToast` strings — error handling for a state nothing can produce, which is what the working agreement's "no error handling for impossible scenarios" names. **What stays:** the guard itself, checking the WHOLE envelope. Its invariant is *never write what you cannot read*; it is what turned a silent unrestorable file into a refusal at the moment of writing, and it outlives whichever rule breaks it next. If it ever fires it now reports the parser's own first issue — the honest answer when by construction it should not have fired. **Supersedes** D127's framing: the pre-#31 deadlock is not an accepted cost, it is an unreachable state. D127's RULING — the count is required at the backup importer — is untouched and is one of the four reasons the state is unreachable | 2026-09-01 |
| [D129](D129.md) | **The transaction form asks for an asset only on the types that have one, and blanks the field on the two that do not.** Owner's bug report, 2026-09-02: «внесок» could not be entered correctly. **The domain has had two classes of row since P1 and the form knew only one** — `Transaction.assetId` documents `''` for portfolio-level rows, `lib/seed.ts` writes exactly that for its three deposits, `backup/json.ts` skipped its referential check for `''` (it now branches on the type instead), `derive.ts`'s `portfolioXirr` draws the same boundary citing doc §5.1, and the ledger row already renders it as «Портфель». `transactionObjectFor` carried a bare `assetId: z.string().min(1)`, which asked one question of all nine types, so the form was **the only door that could not write the shape every other part of the app already reads**: a deposit borrowed whichever asset the select happened to be showing (`derive.ts` calls that id noise and steps around it), and with no assets yet — the state anyone is in when they record their FIRST transaction, which is a deposit — the field could not be satisfied at all. **`targetsAsset(type)` in `core/types.ts` is the domain fact**, beside `movesPosition`; `PORTFOLIO_LEVEL` is closed at `deposit`/`withdrawal` (`tax` targets the asset whose payout was taxed). **The refinement REQUIRES an id where one is meant and the transform BLANKS it where none is, and that asymmetry with [`D124`](D124.md)'s units rule is the point:** a refusal has to be shown, and the panel hides this control on exactly those types, so the message would land on something nobody can see. **It also could not be obeyed** — traced 2026-09-02, a freshly mounted Radix `Select` reports its own empty value back through `field.onChange` after an effect writes to it, so "leave the picker empty" is not a state the UI can be held in. Normalizing in the schema needs no timing to be right and cannot be bypassed by whatever the hidden control still holds. **The panel therefore clears the ERROR and never the value**, which is why the user's asset survives a detour through «Внесок» for free; `isNewAsset` gains the same gate, so a deposit cannot open the quick-create panel or run its validation; the ledger row asks the TYPE rather than the id, so a deposit already in the store stops naming a borrowed asset on screen — nothing migrates one, and that was the visible half of the report; and `record`'s reset restores the picker from the SUBMITTING RENDER's watched value — not the parsed one, which the transform has blanked, and not a later `getValues`, because `handleSubmit` awaits the resolver before `record` runs. **W7's schema already forbade what the form wrote:** `transaction_asset_absent_ck` in `infra/schema/user.ts` reads `type NOT IN ('deposit','withdrawal') OR asset_id IS NULL`, and its own comment cited «`schemas.ts` fills `assetId` for all nine transaction types» as a reason to keep the rule one-way — that was a migration which would have rejected every deposit the owner had recorded, and the app was the side that was wrong. **The importer took the rule too, but the STORE's version of it** — `asset-missing-on-position-row` uses `movesPosition`, mirroring `transaction_asset_present_ck`, because the form is stricter than the store on `tax` and both payout types and a backup that refused what the store can hold could not be written at all (the export re-reads its own output — D126's deadlock). Its converse is NORMALIZED there rather than refused: every file written before this carries a real `assetId` on its deposits, and refusing them would make them unimportable for a value W7 discards anyway. **`BACKUP_FORMAT_VERSION` goes to 5** on D122's rule read straight — a v4 build accepts a `buy` with no asset and this one does not, and a v4 build is live because dev deploys on every push. The argument that no build ever WROTE that shape was tried and is wrong: the version tracks the reader, and D128 proved the same of 4 without unmaking it. **It does NOT discharge the W7 migration:** `asset_id` is a uuid and `''` is not one, so item 2 of `w7-migration-translations.md` (translate `''` to NULL) stands; item 3 — the borrowed real id — goes DORMANT, not retired, on the same footing as item 4: only a store written before 2026-09-02 holds one, and D128 says no such store migrates. **Amends** D112's `assetId` contract only where the form is concerned — no STORED row changes. **A pinned figure does move:** the version literal, in `json.ts`, both backup test files, `src/core/README.md` and the navigation map — and after it, no existing backup file is valid, which is the bump's whole cost and is accepted on D113's standing grounds | 2026-09-02 |
| [D130](D130.md) | **An item may cross between plans, and the crossing moves the ROW, not the body.** Owner's ruling, 2026-09-02, given while planning the hygiene branch. `PLAN-WAITING.md`'s own rule has always said an item whose gate has opened *moves to `PLAN-NOW.md` or is executed here directly*, but nothing had ever taken the first branch — **W4 is the first item in this repository to cross**, landing as `PLAN-NOW.md`'s **Section Q**, first in the table by that file's own order (deadline pressure, then irreversibility: W4 gates W7 and W15, and a DSQL natural key is a DROP/CREATE rather than a migration). **Its body did NOT move.** It stays in `phase-w-i-ii-iii.md` beside W3, because W3's answered questions are literally W4's input — the same argument `../plans/README.md` already used to keep W2 beside W6, applied to a move instead of a close. **The row left behind is kept and NOT struck**: it is kept because W7, W15 and `PLAN-OPEN.md` O5 cite W4 by bare number, and unstruck because in that table `~~…~~` already means *done*, which the three struck rows above it are and a moved item is not. **A moved item closes back to the plan it came FROM**, and that half is machine-checked rather than tidy: `plan.closedTasks` in `src/facts/facts.ts` counts the ledger rows of `docs/archive/plan-a/README.md` with a regex whose id shape is one capital letter followed by digits — which **matches `W4`** as readily as `A20` — so filing a W row in Plan A's ledger would silently change a fact fence's meaning from *closed Plan A tasks* to *closed rows of any plan* — in a number three live indexes cite. **One caveat is recorded rather than resolved:** `PLAN-NOW.md`'s header promises no task there is gated on a decision, and W4 owes two — write-every-day against write-on-change, and O5's non-key columns. They are its own first step rather than a wait on someone else, so the row says its FIRST ACT IS TO ASK and Plan C's rule governs: needing an answer means ask, then record it. **It gets an entry rather than living only in `../plans/README.md`, which is rewritable** — the same reason [`D105`](D105.md) was written down, and the reason the code review of this branch refused the README-only version. **Amends** D95's one-index-per-plan shape, which assumed a row and its body never separate | 2026-09-02 |
| [D131](D131.md) | **The logo is a new drawing, and replacing it retired three rules that only made sense for the old one.** The owner supplied a Q-arrow mark — an open `Q`, r32 arc, stroke 13, gapped at the top-right, whose tail is a sand arrow on one 45° diagonal — replacing mark 04's four bars. **mark 04's even-bar-centre rule dies with the bars**: it bought whole-device-pixel edges at 16px because a bar spans `[x-2, x+2]`, and an arc has no edge to align — every tangent meets the grid at a different angle. What replaces it is the viewBox `9.5 8.5 78 78`, **cropped to the ink** (the arc spans [9.5, 86.5] on both axes; the arrowhead reaches x 87.5 and y 8.5), which is load-bearing twice: a 36px sidebar mark becomes 36px of drawing — the diameter the retired disc had — and a 16px tab gets **2.67px** of stroke instead of 2.17px. `mark.test.ts` pins the drawing's own construction instead — asked of the parsed numbers, and ON TOP OF the byte-identical string equality rather than in place of it (that equality is the stricter gate and runs first, so a re-export with compact arc flags fails there; what the geometry adds is the edit applied to BOTH copies consistently, which equality cannot see): true circle (endpoints r32 from the box centre, agreeing with the arc's own `A32 32`), one 45° diagonal (shaft ends, head tip and head-base midpoint all satisfy `x + y = 96`, exactly), perpendicular head base (dot product 0, exactly) — plus a new guard that the component holds **no hex at all**. **The 36px disc behind the mark is retired, amending D56's circle exemption** — the mark's arc IS the circle now, drawn rather than a radius, so there is no `rounded-full` left to exempt. **`--color-brand-sand` is the app's first brand colour and is the SAME VALUE IN BOTH THEMES**, on `pos-on-dark`'s precedent: the mark's only plane is `sidebar-inset`, dark in light theme too, and a flipped pair would put the owner's light-ground `#9c683a` on the LIGHTER of the two plates at **2.67:1** — under WCAG 1.4.11 — where `#d8b494` measures 6.51 light and 8.80 dark. That light value survives only inside `favicon.svg`, whose ground really is a light tab strip. **The wordmark moves to JetBrains Mono ExtraBold at −2%, amending D54 for the wordmark alone — and at 14px, not the lockup's proportional 16.** Measured: at 16px it renders 83.53px and ran **4.93px UNDER** the DEMO badge, whose 78.6px of runway is fixed by the 244px rail; 14px renders 73.09px and restores **5.5px** of clearance, against the 5.9px the D66 layout was tuned to. The cost is stated rather than hidden: the owner's lockup sets wordmark:mark at 28/62 = 0.45, and this is 0.39. **Amends** D56 (the circle) and D54 (the wordmark's face) | 2026-09-02 |
| [D132](D132.md) | **The Inzhur observer writes every day, and O5's remainder needed no new column.** Owner's rulings, 2026-09-02, taken as W4's first act because `PLAN-NOW.md`'s Section Q says that act is to ask. **Write-every-day over write-on-change**, on three grounds and two of them already pinned: `price-archive.md` defines `price_observation` as *per instrument per capture, exactly what the provider served*; its governing rule for consumers is that *a zero delta and an unknown delta must never render the same*, which write-on-change makes identical IN THE DATA rather than merely hard to read; and the cost is ~260 redundant fund rows a year against a tier that reaches 1 GB in ~657 years. W3 measured the shape that made this a question — bonds move seven days a week, fund NAV five, and the funds appear in ALL SEVEN payloads with an unchanged NAV — so under write-on-change two days a week would be indistinguishable from a capture that never ran, and every completeness check would carry a calendar rule for ever. **`NOT NULL` where certain, nullable where the instrument class decides** — and the schema already did exactly that, so **O5's remainder resolves to *no new non-key column is needed* and `price_observation` takes NO DDL at all**: `return_rate_buy`, `return_rate_sell` and `status` have existed and been nullable since A4, and `basis` has accepted `buy`/`sell`/`nav` since row one precisely so this day would not move an immutable key. The irreversible half of W4 was spent correctly a month before W4 started. **One thing the implementation found that no ruling anticipated: `nav: 0` is the provider's NULL.** D31 measured it for two of four funds; the fixture shows every BOND carrying it too, because a bond has no net asset value and the feed fills the field rather than omitting it. Writing those would have put a permanent ₴0 NAV on every bond-day in a table whose role holds SELECT + INSERT and no DELETE. Not generalised to `buy`/`sell`: W3 watched a two-sided quote collapse to a zero SPREAD, which is a real observation. **Two consequences follow for things already built:** `ObservationsWritten` is now dimensioned by source, because undimensioned, Inzhur's daily rows fill in NBU's weekend zeros and a dead NBU observer looks healthy — the graph's stated meaning, *flat through a working week means the derivation stopped*, holds one source at a time; and `diagnose`'s reconciliation took its source from a literal, which was harmless with one source in the table and silently wrong with two, measuring Inzhur refs against NBU's weekend-closed publication calendar and reporting a two-day gap every week that does not exist. **Scope is WIDE for Inzhur where it is narrow for NBU**, and D72 is why: the dealer quote *for every instrument* is what the capture exists to buy | 2026-09-02 |
<!-- /decisions:rows -->
