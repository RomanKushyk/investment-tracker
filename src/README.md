# src/ — Application code

Rules for working in this folder. The binding API contracts (types, repository/hook/store signatures, token names) live in **[`docs/archive/build-plan/pinned-contracts.md`](../docs/archive/build-plan/pinned-contracts.md)** (until 2026-08-26 it was `BUILD-PLAN.md` → "Pinned contracts") — follow them exactly; changing one requires updating every consumer plus a `docs/decisions/README.md` entry.

## Structure

| Path | Responsibility |
|------|----------------|
| `main.tsx` | Fonts, `QueryClientProvider`, `RouterProvider`, `<Toaster/>` (Task 3+) |
| `index.css` | Tailwind import + `@theme` tokens + base styles |
| `routes.tsx` | `createBrowserRouter`: `Layout` wraps 11 eager routes (incl. `/settings`, next-phase P2, and `/transactions`, A32) |
| `app/` | Shell: `Layout.tsx` (owns the two-shell switch and the drawer's state), `Sidebar.tsx` (`SidebarPanel` + the `Sidebar` rail + the `SidebarDrawer` off-canvas box — ONE composition, two layouts, D66), `AppHeader.tsx` (the drawer's stand-in, S2) |
| `core/` | **Pure domain layer** (G1, D8): `types` · `derive` · `money` · `dates` · `colors` · `asset-builder` · `schemas` · `xirr` · `accrual` · `period` · `reminders` · `backup/` · `inzhur/` (+ colocated vitest specs) — see `core/README.md` |
| `i18n/` | **Every user-facing word** (P5/A10, D58): `messages.ts` (`en` canonical, `type Dict = typeof en`, `uk: Dict`; interpolations are FUNCTIONS so their arity is typed) · `useT()` returns the whole dictionary for the stored language · `useDocumentLang()` writes `<html lang>` · `messages.test.ts` asserts at runtime what the type cannot see — a key copied across untranslated, a dropped interpolation, a deliberately shared value |
| `lib/` | Persistence/infra only: `db` (Dexie factory `makeDb(name)`; binds the active dataset's DB — `kubushka` demo / `kubushka-live` — at module init from the persisted `dataset` flag, G4/D16) · `repository` · `seed` · `sync.ts` (P4/D24 — the cross-tab half of the whole-dataset writes: `withDbLock` around replace/clear/reset via `navigator.locks('kubushka-db')`, and `postDbSync`/`onDbSync` over one `BroadcastChannel('kubushka-sync')` object, which is what keeps the acting tab from hearing itself) (+ tests) |
| `hooks/` | `useFormat.ts` — the language-bound formatter (`makeFormat(lang)` from `core/money.ts`); **every figure on screen goes through it** (Contract 0, D58) · `queries.ts` — TanStack Query hooks + mutation invalidation · `useInzhurAssets.ts` — the Inzhur feed's network half (manual-only query, bare GET, last-good `meta` cache, demo-disabled; policy D19) · `useReminders.ts` — `core/reminders` over the stored data for the strip, plus `useReminderToast()` (hosted in `app/Layout`, module-level latch = exactly one toast per app open, D22) · `useBackupDownload.ts` — the one JSON-backup download, shared by Settings→Data and the delete-asset confirm's "download a backup first" CTA (it moved here from `screens/settings/` in A31, when the second caller appeared) · `useDbSync.ts` — the receiving end of `lib/sync` (also hosted in `app/Layout`): another tab's replace/clear invalidates every query and shows the one pinned cross-tab toast (D24) |
| `state/` | Persisted zustand stores: `settings.ts` — **`currency` is the one field here that is deliberately NOT persisted (A21)**: it is what the app is showing right now, written by the sidebar toggle alone, while `defaultCurrency` is the preference that Settings writes and `partialize` carries. `migrateSettings` still reads a legacy `currency` key and must keep doing so forever — the BACKUP FILE format uses that name and was left unchanged. (+ the P3 automation fields `autoQuoteSuggest`/`couponSuggest` (D21), `remindersEnabled`/`reminderLeadDays`/`dismissedReminders` (D22) and `collapsedNavGroups` (A33) — which sidebar nav groups are closed, the deliberate COUNTERPART to `currency` above: a nav arrangement is a durable choice, a currency glance is not), `draft.ts` (quote drafts + the `origins` provenance map — `setQuote` = the user's path and drops the origin, `fillQuote` = the machine's, D20) |
| `components/ui/` | Reusable primitives (CVA variants as they appear; `Scroller.tsx` = the app's ONE scroll surface over the Radix `ScrollArea`, D65 — every box whose size is constrained while its children are not goes through it, and the platform's bar is used nowhere except `Select` (Radix owns that viewport) and the page itself, both dressed to match in `index.css`; `Dialog.tsx` = the S6 modal idiom — `Dialog` plus the `AlertDialog` destructive-confirm variant (D17) — now a three-band grid whose middle band is the only thing that scrolls, exported as `DialogHeader` / `DialogBody` / `DialogFooter`; `Switch.tsx` = the app's one switch anatomy, shared by the AssetForm Inzhur toggle and Settings→Automation; `Reveal.tsx` = the app's one symmetric reveal/hide group, shared by the AssetForm conditional groups and the Settings reminder sub-rows; `ReminderStrip.tsx` + `reminder-labels.ts` = the P3 reminder banners on `/` and `/overview`, their words taken from the dictionary, D22) |
| `components/charts/` | One wrapper per recharts chart (Task 6) |
| `docs-line-cap.test.ts` | **Not app code** — nor are `facts/`, `decisions/`, `claims/`, `distillation/`, `decisions-index.test.ts` and `readme-coverage.test.ts` below. It walks every `*.md` in the repository and fails if a Markdown file already over 200 lines grows past its own committed length (D95, ratcheted by D98 — a file at or under 200 is unconstrained), and pins D96's one-file-per-decision shape. It lives in `src/` because that is where the toolchain runs — `.prettierignore` excludes Markdown, so no other gate step ever reads a documentation file — and that is the same reason `facts/`, `decisions/`, `claims/` and `distillation/` live here too |
| `facts/` | The fact registry behind this repo's `<!--f:app.colorSlots-->4<!--/f-->` Markdown fences: `registry.ts` (`Fact`, `derived`/`measured`, `renderFact`) · `facts.ts` (`FACTS` — `plan.closedTasks`, `seed.*`, `app.colorSlots`, `dpu.observeNbu.window`, `claims.unchecked`) · `markdown-files.ts` (the repo walk `docs-line-cap.test.ts` above, and `decisions/records.ts` below for `REPO` alone, also use) · `fences.ts` (`rewrite`/`rewriteFile`, called by the `pnpm facts` CLI and by `fences.test.ts`'s repo-wide drift check; also exports the `codeRanges`/`inCode` scanner `decisions/render.ts` shares, and the `blank`/`keepOnly` masking glue `claims/` and `distillation/` both share) — see `facts/README.md` |
| `decisions/` | The `docs/decisions/D<n>.md` front-matter codec and the `docs/decisions/README.md` table renderer behind `pnpm decisions`: `frontMatter.ts` · `records.ts` · `render.ts` — see `decisions/README.md` |
| `claims/` | The claim lint behind `claim-baseline.json` (repo root) and `pnpm claim-baseline`: three rule patterns + file-kind dispatch in `scan.ts`, the `.ts`/`.tsx` comment extractor `comments.ts`, the repo walk `target-files.ts`/`repo-scan.ts`, the ratchet in `baseline.ts`, and the repo-wide check `claim-lint.test.ts` — see `claims/README.md` |
| `distillation/` | The distillation ratchets behind `distillation-baseline.json` (repo root) and `pnpm distillation-baseline` — repeated sentences, `.ts`/`.tsx` comment volume (a raw character count, not a percentage), and history-in-the-artifact narration: `scan.ts`, `repo-scan.ts`, `baseline.ts`, and the repo-wide check `distillation-lint.test.ts` — see `distillation/README.md` |
| `decisions-index.test.ts` | **Not app code.** The repo-wide drift/validation check for `decisions/` above — the real README against real front matter, `amends` target/self-reference validation, and a full LF-checkout simulation of `readDecisions` + the splice |
| `readme-coverage.test.ts` | **Not app code.** Fails the suite when `scripts/README.md` misses a file in `scripts/`, or this table misses a subdirectory README or a top-level `src/*.ts(x)` file — the exact gap this row itself once was |
| `components/forms/` | Shared form bodies: `AssetForm.tsx` (standalone create/edit + the fields component the TransactionPanel quick-create reuses; its `InzhurGroup` holds the S7 live ref picker with the manual fallback) + its non-component companion `asset-form.ts` (defaults — which are FORMATTED, so they follow the language like any other figure — options, `inzhurRefOptions`; the error copy is the dictionary's) |
| `screens/` | One component per route (per-route pieces in `screens/<route>/`, e.g. `screens/settings/DangerZone.tsx`); `screens/portfolio/` holds the asset-management half A31 moved off Settings — `useAssetDialogs.ts` (state + writes, a `.ts` because `react-refresh/only-export-components` refuses a `.tsx` exporting only a hook) and `AssetDialogs.tsx` (the create/edit form and the D17 delete confirm), plus `portfolio.ts` and its spec; `screens/settings/` holds the P4 import surfaces — `ImportRow.tsx` (S2 file field + solid drop target, the only way data comes in), `ImportDialog.tsx` (S3 preview/diff + S4 rejected-file report; the Confirm press is the sole write path and the safety backup downloads before it) and `import-labels.ts` (maps the structured issue/warning tokens onto dictionary sentences, D8); `screens/daily-quotes/` holds the fetch ritual — pure `fetch-quotes.ts` (provenance/reconciliation/machine tokens) + `useQuoteFetch.ts` wiring + `FetchQuotesButton.tsx` — and the fixed-yield suggestions: pure `suggestions.ts` (ghost value + coupon prefill over `core/accrual`) + `CouponDueCard.tsx` (the S5 confirm — the phase's only new write path) |

## Hard rules

- **`lib/db.ts` is imported ONLY by `lib/repository.ts`.** Components/hooks consume data exclusively through the repository via the TanStack Query hooks. Machine-enforced by the ESLint `no-restricted-imports` zones in `eslint.config.js`, along with core-imports-only-core (G1).
- **Palette only via `@theme` tokens** (`bg-sidebar`, `text-muted`, …) — no ad-hoc hex in components. Charts take `var(--color-chart-*)` strings from `core/colors.ts` (aliases resolve in `index.css` `@theme`).
- **`--keyboard-inset` has ONE writer: `app/keyboard-inset.ts`**, called once from `main.tsx`, owning that property on the root the way `app/theme.ts` owns `data-theme`. Three surfaces read it — the `/` action bar, the `Dialog` panel's bound *and* centre, and sonner's toast offset — and they read it **from CSS**, so the keyboard moving re-renders nothing. Do not reintroduce a hook: `Dialog` stays mounted while closed (for its exit animation), so a subscription there would put a visual-viewport listener on every dialog in the tree. The `/` bar also publishes its own measured height as `--action-bar-h` while it is up. **The two breakpoints do not line up** — sonner switches offsets at 600px and the app switches shells at 768 — so both of sonner's offsets carry the bar-clearing floor.
- **Secondary text is `text-muted` and nothing else (D68).** Light `--color-muted` is `#696865`, re-derived against `panel` (the reference's `#8b8a86` read 2.88:1 there, against the 4.5 that 9.5–13px body text needs); dark keeps `#9b9a96`. The old `--color-label` is **deleted, not deprecated** — it failed the same surface and solving both landed them on one luminance, so a `text-label` anywhere is a regression, and a new secondary step is a design session, not a token.
- **A window is `core/period.ts` + the `*AsOf` accessors, and nowhere else (A27).** `resolveWindow(option, start, to)` turns a `PeriodOption` into `{from, to, clamped}`, counted back from the LATEST SNAPSHOT rather than from today — today is not a portfolio fact. `quotesAsOf` / `cashAsOf` / `headlineTotalAsOf` are the bounded twins, and the unbounded `latestQuotes` / `latestCash` / `headlineTotal` now DELEGATE to them: one merge, two names. `transactionsIn(txs, window)` is inclusive at both ends and exists so three screens cannot disagree about that. A period means three different things — a FLOW sums over the window, a STOCK is a level at its END, a RETURN needs both ends — and `period.ts` deliberately holds no opinion about which a given figure is. **DEAD SINCE A42, and the sentence was already misleading before that:** all three windowed screens use the bottom-only `transactionsFrom` / `transactionsFromWindow` instead, because clipping the top end makes a row entered since the last saved quote vanish from one screen while every other screen counts it (`yieldTableRowsIn` measured 65 800 against 115 800). `transactionsIn` now has no callers. Left in place rather than deleted — pre-existing, and not A42's to remove.
- **No hard-coded portfolio figures anywhere** — every displayed number derives from stored data (`core/derive.ts`). Placeholders before the data layer exist show "—". **A DATE THAT DIVIDES THE OTHERS IS A FIGURE (A24):** `PORTFOLIO_START` was a literal `'2026-02-03'` until 2026-08-18 and every annualized percentage on `/yield`, `/attributes` and `/overview` divided by it — true of the demo seed and false of every other dataset. It is now `portfolioStart(assets, snapshots, transactions)`, the earliest of the three signals, `undefined` when there is nothing to measure.
- **Two shells, one breakpoint (D66):** `md` = 768px, written as `max-md:`/`md:` in markup and once as a literal in `hooks/useIsDesktop.ts` — they must stay equal. Below it the sidebar is a Radix-`Dialog` drawer and `AppHeader` carries the capital; above it, today's layout plus a collapse control. **44 × 44 is HIT AREA:** `components/ui/tap-target.ts` (`TAP_44`) puts a transparent `::after` around a control without moving its drawn box, and it needs enough SPACING from the caller that two regions never overlap. Only the quote input and `Button` size `md` actually grow (to 44, radius recomputed to 11); `<input>` cannot take the overlay at all, because a replaced element renders no pseudo-element.
- **Shape (D56):** nothing is a capsule. A standalone control takes `round(min(w,h) × 0.26)`; a box nested against a parent's corner takes `outer = inner + gap`; a **segmented control is both** — segment proportional (7), track concentric (`7 + padding`). Surfaces keep 16/20/24. `rounded-full` survives in exactly four places and they are all genuinely round: `AssetAvatar`, `ColorDot`, the sidebar logo circle, the decorative blob — a fifth is a regression. Heights must be MEASURED in the browser: `text-[11px]` sets a font size, not a line height. `Button` radius lives in its `size` variant and every variant carries `border-[1.5px]` (filled ones transparent) so heights stay isometric.
- **Fonts:** `font-display` (**IBM Plex Sans**) for headings/`.btn`-style buttons/KPI numbers; `font-body` (**JetBrains Mono**, the body default) for everything else — swapped 2026-08-12 for Cyrillic coverage (D54); never write a family name as a literal, the token is the only source — note the sidebar nav pills and currency toggle inherit the mono body font (matches the reference markup).
- Formatting/derivation logic lives in `core/money.ts` / `core/derive.ts` (pure, unit-tested) — never inline in components. Per-screen pure glue lives in `screens/<route>/<route>.ts` and imports core only; both return structured tokens, never assembled prose (D8) — the WORDS live in `i18n/messages.ts` and reach the pure layer as a `Dict` argument, never as a module import. (Until 2026-08-14 they lived in per-topic label modules in the component layer; `date-labels.ts`, `yield-labels.ts` and `schedule-labels.ts` were retired by A10 rather than translated, because a context-split formatter cannot express Contract 0.)
- **Language (Contract 0, D58):** no user-facing string is written in a component — it comes from `i18n/messages.ts`, and a figure comes from `useFormat()`. Formatting follows the LANGUAGE, not the context: `68 702,10` / `68 629,36 ₴` / `+3,08 %` / `12.08.2026` in Ukrainian against `68,702.10` / `₴68,629.36` / `+3.08%` / `12 Aug 2026` in English. Two things are deliberately NOT translated: the danger zone's typed confirmation (it is compared with the dataset name, so translating it makes the button unarmable) and the tokens in `SHARED` in the parity test.
- **Motion (D7):** every interaction animates softly — `transition active:scale-[.97]` on pressables, `animate-in` reveals, keyed route transitions. Standards: `docs/archive/BUILD-PLAN.md` → "Motion & interaction standards". The `prefers-reduced-motion` kill-switch in `index.css` must stay.
