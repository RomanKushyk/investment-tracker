# src/ — Application code

Rules for working in this folder. The binding API contracts (types, repository/hook/store signatures, token names) live in **`docs/archive/BUILD-PLAN.md` → "Pinned contracts"** — follow them exactly; changing one requires updating every consumer plus a `docs/decisions/README.md` entry.

## Structure

| Path | Responsibility |
|------|----------------|
| `main.tsx` | Fonts, `QueryClientProvider`, `RouterProvider`, `<Toaster/>` (Task 3+) |
| `index.css` | Tailwind import + `@theme` tokens + base styles |
| `routes.tsx` | `createBrowserRouter`: `Layout` wraps 10 eager routes (incl. `/settings`, next-phase P2) |
| `app/` | Shell: `Layout.tsx`, `Sidebar.tsx` |
| `core/` | **Pure domain layer** (G1, D8): `types` · `derive` · `money` · `dates` · `colors` · `asset-builder` · `schemas` · `xirr` · `accrual` · `reminders` · `backup/` · `inzhur/` (+ colocated vitest specs) — see `core/README.md` |
| `lib/` | Persistence/infra only: `db` (Dexie factory `makeDb(name)`; binds the active dataset's DB — `kubushka` demo / `kubushka-live` — at module init from the persisted `dataset` flag, G4/D16) · `repository` · `seed` · `sync.ts` (P4/D24 — the cross-tab half of the whole-dataset writes: `withDbLock` around replace/clear/reset via `navigator.locks('kubushka-db')`, and `postDbSync`/`onDbSync` over one `BroadcastChannel('kubushka-sync')` object, which is what keeps the acting tab from hearing itself) (+ tests) |
| `hooks/` | `queries.ts` — TanStack Query hooks + mutation invalidation · `useInzhurAssets.ts` — the Inzhur feed's network half (manual-only query, bare GET, last-good `meta` cache, demo-disabled; policy D19) · `useReminders.ts` — `core/reminders` over the stored data for the strip, plus `useReminderToast()` (hosted in `app/Layout`, module-level latch = exactly one toast per app open, D22) · `useDbSync.ts` — the receiving end of `lib/sync` (also hosted in `app/Layout`): another tab's replace/clear invalidates every query and shows the one pinned cross-tab toast (D24) |
| `state/` | Persisted zustand stores: `settings.ts` (+ the P3 automation fields `autoQuoteSuggest`/`couponSuggest` (D21) and `remindersEnabled`/`reminderLeadDays`/`dismissedReminders` (D22)), `draft.ts` (quote drafts + the `origins` provenance map — `setQuote` = the user's path and drops the origin, `fillQuote` = the machine's, D20) |
| `components/ui/` | Reusable primitives (CVA variants as they appear; `Dialog.tsx` = the S6 modal idiom — `Dialog` plus the `AlertDialog` destructive-confirm variant, D17; `Switch.tsx` = the app's one switch anatomy, shared by the AssetForm Inzhur toggle and Settings→Automation; `Reveal.tsx` = the app's one symmetric reveal/hide group, shared by the AssetForm conditional groups and the Settings reminder sub-rows; `ReminderStrip.tsx` + `reminder-labels.ts` = the P3 reminder banners on `/` and `/overview` and their English copy, D22) |
| `components/charts/` | One wrapper per recharts chart (Task 6) |
| `components/forms/` | Shared form bodies: `AssetForm.tsx` (standalone create/edit + the fields component the TransactionPanel quick-create reuses; its `InzhurGroup` holds the S7 live ref picker with the manual fallback) + its non-component companion `asset-form.ts` (defaults, options, pinned error copy, `inzhurRefOptions`) |
| `screens/` | One component per route (per-route pieces in `screens/<route>/`, e.g. `screens/settings/AssetManager.tsx`); `screens/settings/` holds the P4 import surfaces — `ImportRow.tsx` (S2 file field + solid drop target, the only way data comes in), `ImportDialog.tsx` (S3 preview/diff + S4 rejected-file report; the Confirm press is the sole write path and the safety backup downloads before it) and `import-labels.ts` (English copy for the structured issue/warning tokens, D8); `screens/daily-quotes/` holds the fetch ritual — pure `fetch-quotes.ts` (provenance/reconciliation/machine tokens) + `useQuoteFetch.ts` wiring + `FetchQuotesButton.tsx` — and the fixed-yield suggestions: pure `suggestions.ts` (ghost value + coupon prefill over `core/accrual`) + `CouponDueCard.tsx` (the S5 confirm — the phase's only new write path) |

## Hard rules

- **`lib/db.ts` is imported ONLY by `lib/repository.ts`.** Components/hooks consume data exclusively through the repository via the TanStack Query hooks. Machine-enforced by the ESLint `no-restricted-imports` zones in `eslint.config.js`, along with core-imports-only-core (G1).
- **Palette only via `@theme` tokens** (`bg-sidebar`, `text-muted`, …) — no ad-hoc hex in components. Charts take `var(--color-chart-*)` strings from `core/colors.ts` (aliases resolve in `index.css` `@theme`).
- **No hard-coded portfolio figures anywhere** — every displayed number derives from stored data (`core/derive.ts`). Placeholders before the data layer exist show "—".
- **Shape (D56):** nothing is a capsule. A standalone control takes `round(min(w,h) × 0.26)`; a box nested against a parent's corner takes `outer = inner + gap`; a **segmented control is both** — segment proportional (7), track concentric (`7 + padding`). Surfaces keep 16/20/24. `rounded-full` survives in exactly four places and they are all genuinely round: `AssetAvatar`, `ColorDot`, the sidebar logo circle, the decorative blob — a fifth is a regression. Heights must be MEASURED in the browser: `text-[11px]` sets a font size, not a line height. `Button` radius lives in its `size` variant and every variant carries `border-[1.5px]` (filled ones transparent) so heights stay isometric.
- **Fonts:** `font-display` (**IBM Plex Sans**) for headings/`.btn`-style buttons/KPI numbers; `font-body` (**JetBrains Mono**, the body default) for everything else — swapped 2026-08-12 for Cyrillic coverage (D54); never write a family name as a literal, the token is the only source — note the sidebar nav pills and currency toggle inherit the mono body font (matches the reference markup).
- Formatting/derivation logic lives in `core/money.ts` / `core/derive.ts` (pure, unit-tested) — never inline in components. Per-screen pure glue lives in `screens/<route>/<route>.ts` and imports core only; both return structured tokens, never assembled English prose (D8) — label words live in the component layer (e.g. `components/ui/date-labels.ts`).
- **Motion (D7):** every interaction animates softly — `transition active:scale-[.97]` on pressables, `animate-in` reveals, keyed route transitions. Standards: `docs/archive/BUILD-PLAN.md` → "Motion & interaction standards". The `prefers-reduced-motion` kill-switch in `index.css` must stay.
